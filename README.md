# 翻译批改（fanyipigai）

面向「外研社·国才杯」英语组笔译赛项的**个人练习站**。核心不是刷题量，而是把「专家级批改反馈」变成肌肉记忆：你写下译文，AI 以校对符号的方式逐处批注——用底色勾出错处、上方写对的、标红补上漏的、弧线表示语序调换——并给出分数与错误归类。

## 目标赛制的官方规格

以下数字来自公开赛事通知（[2026 校赛通知](https://wgyxy.cuit.edu.cn/info/1060/2390.htm)、[2025 校赛通知](https://gl.wzbc.edu.cn/Art/Art_188/Art_188_131036.aspx)），是本项目所有"字数""文体""评分尺子"的依据：

| 项目 | 规格 |
| --- | --- |
| 英译汉 | 250–350 词，建议 30 分钟，50 分 |
| 汉译英 | 200–300 字，建议 30 分钟，50 分 |
| 考查文体 | 政治文献、新闻编译、文学作品选篇、一般说明文 |
| 术语考查 | 习近平新时代中国特色社会主义思想关键术语、中华思想文化术语的翻译及阐释 |
| 素材来源 | 《习近平谈治国理政》各卷、《理解当代中国》系列教材 |

## 最小修改：按**词**（不是字母）求最小不同项

模型圈的 `oldText` 常比实际改动大得多，而且它常把"漏了一个词"报成"替换"。
程序按单词求最小不同项（算法在 `src/domain/minimal.ts`，接线在 `src/domain/parse.ts` 的 `resolveChanged`）：

**判据只有一条：这处改动是"贴在某个词的内部／词尾"，还是"发生在词与词之间"。**

| 模型说 | 真正的最小不同项 | 页面上怎么画 |
| --- | --- | --- |
| `have explore ways` → `have explored ways` | explore → explored | 勾出 **explore**、上方写 explored |
| `recent year` → `recent years` | year → years | 勾出 **year**、上方写 years |
| `At meanwhile` → `Meanwhile` | At meanwhile → Meanwhile | 勾出 **At meanwhile**、上方写 Meanwhile |
| `over the past years` → `over past years` | 多了一个 the | 勾出 **the**、不写任何东西 |
| `over past years` → `over the past years` | 漏了一个 the | **一个字都不勾**，在 past 前面补 `<the>` |
| `farmer and herder` → `farmers and herders` | farmer → farmers、herder → herders | **两处各画各的**，中间的 and 不划 |
| `aaa bbb ccc dddd eee` → `hh bbb ccc dddd dee` | aaa → hh、eee → dee | **中间那三个词不划**（第 6 条：雷同片段不标色） |
| `In wake of reform` → `Since the beginning…` | 多处变化，缩不了 | 整体替换 |

四条规则值得单独说：

1. **替换一定按词对齐**。`At meanwhile` → `Meanwhile` 的字符级差异是 `At m` → `M`
   （只差一个大小写字母），但用户要的是"这个词写错了"，所以勾出的是整个 `At meanwhile`。
2. **词与词之间的增删，一个字都不勾**：漏的词用尖括号补（`<the>`），多的词直接勾出——
   这正是校对符号的写法。早期版本不看形状，一律"朝两侧逐步比对、相等就让出一步"，
   于是 `past → the past` 被扩成"勾出 past、上方写 the past"：只是漏了一个词，却像整个词被换掉。
3. **一处错误可以落在好几个词上**。`farmer and herder` 写成 `farmers and herders`
   该改的是两个词（and 没错），页面上各画各的；但它们共用同一个编号，
   点哪一边选中的都是**同一处错误**（卡片也只出一条）。
4. **合并两块的判据是"隔开它们的是什么"**：只隔空格或标点（`i is` → `I am`）→ 合成一处；
   隔的是词内字符（`plant` → `afforested` 被字符级差异切出来的 `a`、`t`）→ **也是同一个词，必须合**；
   隔着一整个词（`farmer| and |herder`）→ 绝不能合。
   合并这一步曾经只看"隔开的是不是词字符"，于是 `plant → afforested` 被切成好几块、
   重建不回去，最后整句勾出——这正是"只改了一个词却勾出整句"的来源。
   另外一处批注最多容纳 **2 项**改动，再多说明新旧文字牵连太多，按整段替换标
   （`In wake of reform → Since the beginning…` 会被切出三项，那就该按整句重写标）。

   同一件事的另一半：「整句重写」只有当最小不同项**正好覆盖模型圈的那一整段**时才算——
   模型把"只改一个词"报成 rewrite 时，照样只勾那一个词。

### 雷同片段不标色：改动块两端那截相同的内容也不划（第 6 条）

用户的原话："对于中间内容相同的且相同部分大于两个字或者单词的雷同部分，也不标色。"
追问之后他给了更硬的口径：**每个单词就是最小单位，绝不许把一个词按字母拆开来标**。

落地成一条**最终裁剪**（`MIN_SAME_UNITS = 3`）：改动块的**开头或结尾**若有一截"两边逐字相同、
达到 3 个单位、而且切点在两侧都落在单位边界上"，就把那一截从块里剪掉
（`start/end` 与要写入的新文字同时剪，因此"应用回去能还原"这条不变量照旧成立）。

- **单位怎么数**：英文只数**完整单词**（`At meanwhile → Meanwhile` 里那截 `eanwhile` 是半个词，
  一个单位都数不出来，因此**照旧整段替换**——这条用例一个字没改）；
  中文按**字**（没有空格，所以还得把汉字排除在"词"之外，否则一整句会被当成一个词）。
- ⚠️ **用户举的那个英文例子改前就已经是对的**（字符级 LCS 本来就把中间三个词留在块外，
  实测改动前后输出一致）。真正吃到这次改动的是**中文**：`他们讨论了中国农业发展的问题`
  → `他们介绍了中国农业发展的问题` 原先**整句标色**（中文没有空格，`alignToWord` 把一句当成一个词），
  现在只标「他们讨论 → 他们介绍」。
- ⚠️ **边界（有意保留的例外）**：一处改动最多容纳 2 项，超过就退化成**整段替换**；
  那种句子里相同的文字夹在**两处**改动之间，裁剪只剪块的两端、本来就剪不到它。
  唯一能让那种句子只标真正变的词的办法是把上限调大（实测上限 ≥4 时中间那三个词一个都不标），
  但那是**对照视图**的口径（两行并排、越细越好）——批注是画在译文上的，宁可整段重写，
  也不要一堆互相牵制的碎片。这条边界钉成了一条用例，名字里就写着"若有意的改动请先改这条"。

译文上要写的文字同样取自这处最小不同项，因此不会出现
"只勾出 a、上方却写一整套 became an important"这种重复。

`src/domain/minimal-cases.ts` 里固定了 **26 条用例**，**每条都会把结果应用回原文验证能还原**，
由 `npm run smoke` 一并执行。这个算法很脆，改动前请先看这些用例。

> ⚠️ 三条踩过的坑，改这里之前务必先看：
> 1. 判"原文被完整保留、只多出一截"必须发生在**算出范围之前**，而且只能判**纯插入**（多出来的全是标点/空格）。
>    早期版本先把 `targetText` 两端重复的原文削掉、再算差异，结果词形变化（`live conditions`）
>    被削得只剩后缀 `s`，差异算法退化成整段替换，把整段都划了出来。
> 2. 按词扩范围时，只有"贴在词内部／词尾"的改动才该扩；词与词之间的增删一扩就变成整词替换。
>    但**替换**（两边都有内容）必须无条件按词对齐——否则 `At meanwhile → Meanwhile` 会只标 `At m → M`。
> 3. 切出多个最小不同项之后，要同时满足"各块互不重叠"与"应用回去能还原成新文字"，
>    否则整段替换。只判"能还原"是不够的：`In wake of reform → Since…` 会切出三个互相压着的块，
>    靠逆序应用居然能凑巧还原，但画在页面上是三条互相压着的荧光笔带子。

## 定位方式：AI 不数序号

**AI 从来不给字符序号。** 它只说明"要改哪段文字"，位置由程序自己找（`src/domain/locate.ts`）。

```json
{ "type": "replace", "category": "function-word",
  "oldText": "i is", "targetText": "I am",
  "explanation": "第一人称代词 I 必须大写；主语 I 搭配的 be 动词是 am。" }
```

程序拿 `oldText` 去译文里找位置；同一片段出现多次时，模型可以补 `contextBefore` / `contextAfter`
（左右紧邻的几个字）或用 `occurrence` 指定第几次出现。找不到或分不清时**不猜、不将就**，
直接退回重试并说明原因。

为什么这么做：早期版本要求 AI 给出 `start` / `end`，实测里**数错一个字符是最常见的失败来源**。
改成按文字定位后，这类失败从根上消失了，提示词也短了一大截。
而"位置"这一步并没有丢——它由程序算出来，写回 `anchor` 字段，
因此界面、校验、排版这些下游逻辑完全不需要知道定位方式变了。

## 功能结构

- **两大方向**：中译英、英译中
- **四类题型**：术语翻译、句子翻译、段落翻译、文章翻译
  - 数据上各自独立（术语题没有"语序调换"，文章题要管篇长与段落），**共用同一个批注渲染器**
- **术语题走另一条渲染路径**：一页给**5 条术语**（上五栏原文、下五栏作答），
  五条都写上才能提交，提交后**由程序对照官方译名本地判分**——不调 AI、不用等、不花钱，
  可复现也可解释（见 `domain/terms.ts` 与 `domain/term-exercise.ts`）。
  版式是**五条术语在原文栏里上下五等分、用分割线隔开**，作答栏是**五个等分的输入框**、
  用同一套分割线（两栏的横线对得上），**作答栏里不再重复出现原文**，输入框**不画边框**
  （上下已经由分割线给出边界，再加方框就是三道线挤在一起）。
  末页不满五条时**照旧五等分**，缺的行留空、不可填也不计分（用户拍板）。
  「提交批改」在**译文栏标题栏的右边**（与文章模式同一个位置），这一页的术语都写上才可点；
  判完之后**同一颗按钮改名叫「返回编辑」**（与文章模式同一颗按钮、同一个动作，见下）。
  判完之后**不是"锁住输入框"那副样子，而是与文章模式同一套颜色批注**：
  译错的整条加相应颜色的荧光底色 + 一道横线划掉、同一行右边跟一个「→ 官方译名」，
  译对的用绿色底色；点任意一条在右下角看那一条的完整说明（"要改的是…改成…"），
  收藏取到的就是那一条那一行，切到**对照视图**则是"一条原译、一条改后"逐条对照。
  判分口径**不是"一字不差"**（第 13 轮放宽，见 ADR 0024）：官方缩写（`NPC`／`UN`）、
  英美拼写（`Organisation`/`Organization`）、重音符号、开头的 `The` 都算对，
  **一英多中**（`直辖市人民政府` 与 `设区的市人民政府` 的官方英文是同一条）时两个都算对、
  屏幕上用「／」一起列出来；但 `whole-process people's democracy` 与
  `whole-process democracy` **仍然只认前者**——那是同义替换，不在放宽之列。
- **术语栏的分组是「术语范围」**（第 13 轮）：两张卡片——**国内机关名称**（83 条 · 17 页）、
  **国际机关名称**（60 条 · 12 页），材料是仓库根目录的两份 docx，正文由
  `node scripts/build-terms.mjs --write` 现算（与文章库同一套做法，见 ADR 0023）。
  「范围」按钮与「中译英／英译中」两段按钮都在**原文栏标题栏、紧挨「原文」**（与文章栏同一位置）；
  点「范围」弹出**两屏**：先两张范围卡片，点一张进大卡片——范围名 + 条数页数 +
  一张可滚动的**中英对照平表**，底部「确定」（切过去并**落到第 1 页**）与「返回」。
  ⚠️ 那张对照表**就是答案表**（用户拍板"中英文都列出来"，并且练习中点「范围」也能重新打开它）。
  术语栏**没有「换一换」、也没有「AI 出题」**：术语只来自这两个范围。
  方向两侧都能练（每条术语中文名与官方英文名都有），因此两个方向**永远可点**。
  术语栏与文章栏的**分组表是两张平行的表**（术语范围 ≠ 话题领域），见 ADR 0025。
- **顶部导航栏**：`文章` `句子` `术语` 三个题型，加上 **`自定义`**（自己贴原文来练）
  与独立的 **`练习记录`** 一栏，以及 **`收藏`**；最右边是 `暗夜/日间` 与 `设置`。
  > 「段落」栏**已从导航撤掉**（文章题本来就按自然段切、逐段作答，另开一栏是重复）。
  > 类型、内置题目与相关代码都留着，想恢复只需去掉 `MODE_TABS` 里那一条的 `hidden`。
  > ⚠️ 顶栏原先还挂着一枚「内置示例批改」警告芯片，**第 13 轮整枚删掉**（用户要求）——
  > 它的触发条件之一是术语判分（那时术语判分借用了 `fixture` 这个来源），
  > 于是**每次术语批改都冒出来**。现在术语判分有自己的来源 `local`，
  > 示例批改的提醒搬进了**结果栏**（见 `AnswerPane` 那枚「示例批改」芯片）。
- **「句子」栏那一行**只给**领域下拉** + 「换一句」：题目从该领域的文章里**本地切句**
  （按句末标点切，取够长的句子），因此**不能选具体文章、也不给方向**——
  方向由句子本身是中文还是英文决定（领域里两个方向的句子都有）。
  切句不调 AI：不花钱、不用等，且同一个题号永远是同一句。
- **「文章」栏那一行**是文章库的入口：左边**领域下拉**（**五个板块**：社会／经济／文化／生态／科技）、
  中间**中译英／英译中方向切换**、右边**「选择文章」**。点某个领域会**接着弹出选文章的框**，
  里面以卡片罗列该领域该方向下的文章（**标题就是那一篇的「事件锚点」+ 全文篇幅 + 共几页**），
  点一张即开始练。卡片上**没有"来源媒体"**——这批材料不是新闻原文，编不出真实出处就不要编（见 ADR 0011）。
  **练完的排到最后**并挂一个「已完成」（用户要的是"以后『换一换』留到最后"）。
  没有文章的那一侧方向是**禁用的**，不会让人点进空白。
  > 「事件锚点」是那篇材料自带的**一行背景说明**（例如"2026 年 3 月，十四届全国人大四次会议
  > 民生主题记者会披露社区养老服务扩面计划"），它同时充当标题。练习页上**不显示**它——
  > 练习页只有原文，那才像考场。
  > 这一行取代了原来的「一排题目按钮」——题目一多就横铺不完。段落/句子两栏仍用内置题库、
  > 术语栏用自己的两个范围（见上），因此不显示这一行；「设置」留在顶栏，否则那三栏就没有设置入口。
- **主体是四栏 2×2 布局**（左右均分；上下**默认各占一半**，并按内容让位）：

  | | 左 | 右 |
  | --- | --- | --- |
  | **上（2）** | 原文（带「换一换」与「AI 出题」按钮） | **带批注的我的译文** |
  | **下（1）** | 总体评分 | **批注详情**（只显示点中的那一处） |

  - **批注画在译文上**：勾出的词（荧光笔底色）、上方的小字写正确写法、插入标记、以及语序调换的配对弧线，
    全都直接长在右上角你自己的译文上——这是最主要的信息载体
  - **点哪一处，看哪一处**：点译文上的任意一处勾画（标记、上方小字、插入的下划线、调序弧线），
    那一行旁边浮出**小卡片**（显示完整说明，不截断）；右下角同时换成**这一处**的**大卡片**
    （哪段文字 → 改成什么 → 为什么），**不一次性铺开全部错误**
  - **点两张卡片里任何地方都不会把它们收起来**（用户要求："点击任意卡片都不会关掉这两个卡片，
    当且仅当点击这两个卡片之外的地方才消失"）：两张卡各带一个 `data-card` 标记，判定按整张卡走。
    从前只放行了卡片里的标题与按钮，点到大卡片的**正文**（说明那几行）就把它关掉了
  - **小卡片是一块浮层，压得住下面那两栏**（用户要求）：它用**固定定位挂在 `document.body` 上**，
    而不是画在译文栏里面。译文栏的正文盒子是滚动容器，滚动容器会**剪掉**超出它的内容——
    一处靠近栏底的批注，卡片画到一半就被切口截断，看着正是"被下面的栏目挡住了"。
    剪裁不是 z-index 能解决的，只能把它挪出那个容器（z-index 也一并排好：
    四栏 0 < 填补层 5 < 小卡片 20 < 顶栏 30 < 批改完成通知 50 < 弹窗 100）。
    下方放不下时它会**翻到那一行上面**（箭头跟着挪到卡片下沿），免得整块落到视口外面
  - 点两张卡片之外的任何地方、或按 Esc，卡片就收起来；再点同一处也能取消
  - 右下角**标题栏的右侧**有一行小字「点击查看 AI 返回完整内容」，
    点开后屏幕中央弹出窗口，原样显示模型返回的**全文**（未经解析、未经收窄）
  - 一处错误落在好几个词上时（`farmer and herder`），译文上各画各的，卡片仍只出一条
  - 因此右下角永远只有一张卡片，不用在清单和译文之间来回找
  - **上下两排的高度是动态的**：默认各占一半（下方两栏的顶边正好在二分之一处）；
    原文或译文很长时，上排自然多占、下排让位，但**下排最多占到一半**（再多就自己内部滚动）、
    **最少留 180px**（挤到头就不动了）。上排也有 200px 的下限，不会被下排顶没
  - **四个区域的边界可以直接拖**：拖动分隔条调整左右宽度 / 上下高度，**双击分隔条恢复自动**。
    **练习记录页的三条分隔条也都能拖**（外层左右、右边那一屏的上下、以及上排的原文 / 译文），
    双击一样恢复自动。注意手动模式那几条规则必须写成**子选择器**（`.split-manual > .split-row`）：
    记录页是嵌套的两层 split，写成后代选择器时外层的列模板会漏到内层去，
    内层两个 pane 被按三列排，译文那一栏会被塞进 8px 的分隔条列——
    表现就是"拖动之后译文每行只剩一个字符"（实际踩过这个坑）。
    手动调过之后就按你定的比例，不再自动让位——手动的意思就是手动
  - **译文那一栏有两种视图**，用标题右侧的分段按钮切换（**练习记录页的「当时的译文」也有这同一对按钮**，
    两边共用设置里那一个 `answerView`）：
    **批改视图**（默认）= 在译文上勾画（荧光笔底色、上方补写的字、调序弧线），点哪一处看哪一处；
    **对照视图** = 一句一句对照——每句下方给出**修改后的完整那句**，不划线、不填补、不加粗、不折块。
    版式是**一句译文、一句改后译文交替排版，各占一行**（长句在内部自动折行），
    行首带「原译 / 改后」小标签。
    两行的分工：**原文那一行**里被改过的那一段加**荧光底色 + 同色文字**（荧光只出现在原译文上），
    **修改后的那一行**只给文字上色、不加底色。点两行里任意一段都能在右下角看到那一处的说明；
    改动的字用行内 `span` 而不是按钮，因此长内容会像普通文字一样在**内部自动折行**
  - **切句的判据只有一份**（`domain/sentences.ts`）：对照视图、句子题的「换一句」、收藏的"整句"
    都调它，差别只有 `splitAtCommas` 这一个开关（**只有收藏把逗号也算句末**）。
    句末标点后面**紧跟引号**也算断句，因此 `xxxxx  "xxxx."someone says` 一定切成两句；
    **句号后面漏了空格**也算断句（`…livelihood.The meeting…` 是三句不是一句）——
    用户第三次报"对照视图还是一段一段的"就是这个：他的译文里两个句号都粘在下一句首字母上，
    整段因此只出一行。这与提示词同一个口径（AI 不许因为少一个空格扣分）；反过来，
    `U.S.A`（点号前面是大写字母）与 `3.5`（点号后面是数字）照旧不切。
    收藏那边顺带补了一条：`82,000` 里的千位分隔符不是一句的结尾

  - **批改视图的译文行距就是设置里那个值**（范围 1–3，默认 1.6）。
    对照视图是"一句对一句"，不受行距设置影响
  - **导航栏有「设置」**（存在浏览器本地）：正文行距（滑杆）、是否显示填补的正确写法、
    译文默认用哪种视图
  - 四块各自独立滚动（**都不显示侧边滚动条**，直接滚轮/触控板操作），互不干扰
  - **批过的页是只读的**：想改要先按「返回编辑」——批注是按**提交当时**那段文字算出来的位置，
    改一个字就全对不上了。放开之后这一页要**自己再按一次「提交批改」**（翻页不会替他提交）
  - **退回修改之后仍能回到批改界面**（详见下面「文章模式逐页批改」那一节）
- **「参考译文」**（第 7 条改过）：**原文正文结束后那四个字**，点一下把**当前这一页**的译文
  铺在原文下面、再点一下折回去。没有参考译文的题（自己贴的、句子题、术语题）连这四个字都不出现。
  ⚠️ 早先它在**原文标题栏**是一颗「对照译文」开关，按一下把整页铺成"一段原文、一段译文"交替；
  用户要求删掉那颗按钮、改成正文末尾的可折叠块（"原文结束后有『参考译文』几个字，
  点击后，原文下方添加参考译文的内容，点击相同位置，参考译文折叠回来"），
  因此那条交替铺开的渲染路径**整条删掉了**。练习记录页用的是同一个折叠块。
  ⚠️ 参考译文**只给人看、从不发给模型**（ADR 0003 的补充）：批改请求里只有原文与你的译文。
- **领域与方向搬进了原文标题栏**（第 7 条）：`原文` 三个字右边**紧挨着**就是领域下拉，
  再往右是「中译英 / 英译中」两段切换（只在该领域有该方向文章时可点）。
  原先它们占着原文栏**上方的一整条横条**（`.article-bar`），用户要求"整条删掉，方向切换也一起进原文标题栏"，
  因此那条横条整个没有了。句子栏只给领域（方向由句子本身决定），自己贴的题两个都不给。
  ⚠️ **术语栏在第 13 轮也有了这么一对**（「范围」+ 方向，位置一样），但那两个控件与领域无关：
  它管的是**术语范围**那张平行的表（见功能结构那一节与 ADR 0025）。
- **「上一页 / 下一页」固定在原文栏的最下方**（第 8 条）：它原先跟着正文一起滚，
  一页很长时要滚到底才够得着；现在它是这一栏的**页脚**（`.pane-foot`，不在滚动区里），
  正文自己滚、按钮永远在同一个位置。
- **「换一换」**：原文标题右侧的按钮。
  **文章栏里它是"换到下一篇"**（同一领域、同一方向；练完的排到最后，见下）；
  其余题型是"换成同话题、同文体的另一篇原文"，备选篇目有两个来源，按位轮换：
  手写的 `src/domain/variants.ts`，以及**你自己用「AI 出题」现出的**。
  这道题只有一篇原文（且文章栏这一格只有一篇）时按钮是禁用的。
- **「AI 出题」**：在「换一换」右边。点开选**方向 / 文体 / 领域**（领域可选预设，也能自己输入），
  AI 会现出一篇**完整文章**并配好参考译文；生成结果**按题目留存**，之后用「换一换」随时翻回来。
  篇幅由程序**硬校验**（英译中 250–350 词、中译英 200–300 字），不达标就带着原因退回重写。
  非文章题型从这篇全文里**截取对应大小**：段落取第一个自然段、句子取第一个句子、术语取文章里的关键术语。
- **文章模式逐页批改**：原文按**页**拆开（**一页 = 一个自然段**，不足 50 单位的与相邻段合并，见 ADR 0012），一次只显示一页，
  **页下（左边原文那一栏）有「上一页 / 下一页」**。规则如下（第三版起，见 ADR 0017）：
  1. 点「上一页 / 下一页」**只是翻页**：不提交、不拦人，当前页的草稿按页留着，回来接着写；
  2. 提交只有**一个**触发点——用户按「提交批改」。写完一页不按就走，那一页会一直停在"待批改"；
  3. 已经批过的页翻回去**只看结果，不重新提交**（批改要十几秒，还可能给出不一样的结果）；
  4. 批过的页**只读**，放开重写要先按「返回编辑」；放开之后**结果作废的时机只有一个**：
     用户真的改了一个字（改回原样不算）——"放开看一眼、一个字不改又翻回来"不会白花那次调用。
     「返回编辑」这个状态**只管你人还站在这一页上的时候**：一个字没改就翻走，
     标记被收回，切页回来看到的仍是批改界面（用户要求："假如当前页面处于批改后的状态，
     那么切换其他页，再切换回来时，也需要在批改界面，不能回到编辑界面"）；
  5. **一次只批一页**：批改期间提交按钮按不动（正在批的那页写着"批改中…"，别的页上注明原因）。
  结果**按页存**（`pages: 页号 → 那一页的批改`），所以翻回第 2 页看到的是**当时**那次结果。
  没有"提交全篇"这回事了。
  6. **提交前有两道门**（第 3 条，见 ADR 0018）：文章题与段落题**必须多于 30 个单位**
     （中译英数词、英译中数汉字），而且**不能与这一段之前任何一次提交一字不差**
     （去掉首尾空白后逐字符比较）。撞上任何一条都**不提交**，只在底部弹一条**吐司**说明原因
     （"还差多少"／"与之前某次一字不差"）。删掉的那条记录不再拦人。
  7. **那颗按钮在两种状态之间换的是名字，不是位置**（第 2 条）：批改后它写「返回编辑」，
     点下去回到可写状态，名字随即变回「提交批改」——同一颗按钮、两个名字
     （以前是"这颗消失、另一个位置冒出一颗"）。
  8. **「返回编辑」不再撤掉进度**（用户拍板"进度只增不减"）：批过就算批过，
     那一页不会被从"这一篇批过哪几页"里撤走。只有**删掉这一页最后一条记录**才会撤
     （见 ADR 0019 的连带后果：整篇批完后仍算练完，下次打开会落到同格下一篇）。
  > ⚠️ **术语栏第 13 轮起整套照搬这一节**（一页五条、翻页不提交、批过的页只读、
  > 「返回编辑」与「提交批改」是同一颗按钮、进度也管"没批完的那一页"）。
  > 差别只有一处——术语判分是**本地**的：结果是瞬时的，因此不会有"批改中"那一档、
  > 那条进度条与那条「批改完成通知」。术语栏的"练完的不管它"是**换到另一个范围**去
  > （国内 17 页练完之后，下次打开术语栏会落到国际机关名称上）。
  - **交出去之后这一页只读，但翻页不拦**：提交后作答框立刻 `readonly`（这一刻改字，回来的批注就画错了），
    按钮变成"批改中…"，进度条开始走；**与此同时「上一页 / 下一页」照旧能点**——
    用户可以先去下一页接着译。因此"正在批改"记的不是一个布尔量，而是
    **`judgingTarget = {题号, 页号}`**：只有它才同时答得出"这一页在批吗"（进度条、只读）
    与"别处占着这次调用吗"（提交按钮禁用）
  - **提交之后弹一个等待窗**：「批改大约需要一分钟。等待过程中可进入下一页继续翻译……」，
    两颗按钮「停留此页」「进入下一页」（关掉它批改照旧在跑）。**末页与单页题不弹**：
    后面没有下一页可去，弹出来只有一颗"知道了"能按
  - **批完在右下角喊一声**：「第 x 页已经批改完成」，**点它跳到那一页**看结果；
    人自己翻到那一页时它自动消失（已经站在那儿了）。同时只可能有一条——一次只批一页
  - **进度会记在浏览器里**（`article-progress`）：**哪几页已经批过**。于是
    **下次打开就从没有翻译完成的那一段继续**；整篇练完的文章**不再被主动打开**，
    在「选择文章」与「换一换」里都**排到最后**并标「已完成」（点开照样能再练一遍）。
    ⚠️ 这份进度**只增不减**：按「返回编辑」不会把它撤掉（用户第 2 条的追问），
    只有删掉这一页最后一条记录才撤（ADR 0019）。
    ⚠️ 原文栏那枚「已批 N 页」芯片与「官方建议 N 分钟」芯片**都按用户要求删掉了**——
    删的是**显示**，进度照旧在管落点，`verify-article-bar.mjs` 里改成直接量落盘的进度
  - **另外还记"上次关掉网页时停在哪"**（`last-view`，见下）：**栏 + 题 + 页**。
    它比"从没批完的那一段继续"**优先**——用户要的是"下一次落到上一次关掉时的界面"；
    只有没记住（第一次来、换了台电脑）时才回落到进度里那一页。
    ⚠️ 「练完的不主动打开」这条**优先于**记忆：上次那篇整篇练完了，打开时照样换到下一篇没练完的
  - **交给 AI 的原文只有当前这一页**，不是整篇。逐页批改本来就是"一页一页交"，
    把整篇发过去只有坏处：模型容易去评别的段落、白花输入 token、与"只批这一页"在语义上也不一致
  - ⚠️ **提交时作答的起点必须是 0**（`answerSections[0].start`）：服务端会按它把批注**平移**后再返回，
    而界面拿"这一页的文字"画勾画。填成"这一页在整篇里的位置"会让第 2 页以后的批注整体错位
    （勾画一处都画不出来，分数却照常显示）。实测见 `scripts/probe-section-offset.mjs`，
    断言在 `verify-per-page.mjs` 里
  - **批改时有一条细蓝的进度条**（标题栏下方、作答框上方）：按台阶表**一格一格跳**上去
    （不平滑、每跳 2 个百分点）：**1 分钟到 80%**，之后明显变慢，**1 分 20 秒到 98%** 就停住，
    结果回来随即消失。它估的是**时间不是真进度**（接口只有"发出去"和"回来"两个时刻），
    因此不显示百分比数字；实测单次批改约 10–35 秒，所以多数时候结果会在 80% 之前就回来，
    这条节奏主要是为"慢的时候"准备的——**宁可慢一点爬，也不要十几秒冲到头再僵住**
  - **「批改记录」下拉**（译文栏标题栏，见 ADR 0016）：列出**这一页之前每一次批改**
    （最新在前，标"第 N 次 · 时间 · 分数"），选哪一次就把右栏切成那一次的结果——
    **只读、不发请求、不动正在写的文字**，旁边给一个「回到作答」回来。
    它读的是**落盘的练习记录**，因此**改过字之后照样能回看**（这正是它取代旧「查看上次批改」的理由：
    那颗按钮只在"一个字都没改"时才出现，一动手改字就再也看不到上次批的是哪儿了）。
    它同时取代了原来那个「已改过 · 待提交」提示芯片（用户要求去掉）。
    **只读的已批改状态下它也显示**——批完不再自动翻页，人就停在这一页，
    要回看前几次不该逼他先按一次「返回编辑」
  - **提交成功会把这一页收回只读**：以前不是这样——「返回编辑」过的那一页再也回不到"已批改"这一档，
    用户改完按了提交，界面却仍旧停在作答框上，而新结果明明已经存下来了。
    现在提交成功就重新只读、把刚批出来的结果摆出来；想接着改，再按一次「返回编辑」（见 ADR 0016）
  - **批改过的段落，回来还是批改界面**（用户报过两次）：按过「返回编辑」但**一个字没改**就翻页或换栏，
    这个标记会被收回（"「返回编辑」只管你人还站在这一页上的时候"），回来看到的仍是那份批改；
    改过字再走则保留——那时结果已经作废，它是一道真正待提交的题
  - **刷新之后也一样**：会话（作答、结果、页号）只在内存里，而**练习记录是落盘的**，
    因此打开一道题时会把"记录里有批改、会话里没有结果"的那几页**接回会话**（每页取最新一条，
    草稿一并补上）——刷新一下、或者页面被热更新重载一下，批改过的段落仍显示那次批改，
    按「返回编辑」拿回来的也是当时写的那段文字，而不是一张空框。
    接回只在**这道题本次打开还没有会话**时做，因此不会把用户刚作废掉的结果复活，
    也不会盖掉正在写的草稿（`sessionRestored`，只认会话里没有的那几页）
  - **切栏再切回来，看到的东西不变**：连"我正在看这一页的第几次批改"这条记忆也一起留着
    （用户报过："批改界面下，切换导航栏后，回到文章模式时，不要恢复到批改前的空白状态"）。
    ⚠️ 第 10 条之后这条记忆**按「题 + 页」落盘**（`components/page-state.ts`），因此
    **翻页往返、刷新、重开浏览器都还在**——早先它是一个单数 state，**翻页就被清掉**，
    于是每次翻回来都退回最新一次（用户报的正是这个）。解析不出来时自然落空
    （换了题目 / 记录被删掉或裁掉——判据按"当前题目 + 当前页"过滤）
  - **刷新之后旧批改不再挂在页上**（第 10 条的追问，见 ADR 0019）：
    落盘里记着"这一页在编辑态"的那几页，刷新后只接回**草稿**、不接记录里的结果——
    用户的答复是"不算了，自己去「批改记录」下拉栏里重新调出来"。
    **没被「返回编辑」碰过**的批改页，刷新后仍然是批改界面（这一条 ADR 0017 的规矩没变，
    `verify-per-page.mjs` 里两条都量）
- **每一页的界面状态落盘**（第 10 条，`translation-practice.page-state.v1`）：写到哪儿的**草稿**、
  是不是在**编辑态**、**正在看第几次**批改，三样都按「题 + 页」存在浏览器里。
  它有 200 页的上限，超出丢最久没动过的；换原文（「换一换」/ AI 出题）时整道题清掉
- **分割线的位置也记得住**（第 5 条，`translation-practice.split-layout.v1`）：
  练习页与练习记录页那几条可拖动的边界位置都落盘，下次打开还在原处。
  **双击恢复默认也写回存储**——用户的口径是"你走的时候什么位置，下次回来还是在那个位置"，
  因此"自动布局"本身也是一个要记住的位置

> ⚠️ **上面那些键的前缀 `translation-practice.` 是数据地址，不是项目名——改名时故意没动它。**
> 项目在 2026-09 改名为 `fanyipigai`／「翻译批改」（仓库名、页面标题、启动脚本一起换），
> 但 localStorage 是**按键名取数据**的：前缀一改，所有人已经存下的练习记录、草稿、设置、
> 收藏、分割线位置**全都读不到**，用户看到的就是"记录凭空没了"。
> 要改只能先做一次迁移（读旧键 → 写新键 → 删旧键），收益只是好看，因此不做。
- **批改记录可以删**（第 11 条）：下拉里每一条右边一颗**叉号**，点它先就地确认一次，
  删掉之后**练习记录里同步消失**——两处读的本来就是同一份数据，不需要第二份代码。
  练习记录页也多了一个「删除这一条」（同一个函数）。连带：删完撤掉**文章进度**里的那一页；
  正在看的就是这一条时退回最新一次（一条都不剩就退回作答框）；屏幕上正摆着这一份结果时换成剩下最新的
- **文章卡片上有「最高分 N 分」**（第 12 条）：这篇文章历次批改里最好的一次（按题号取最大），
  没得分就写 **0 分**（不是把标签藏起来）。**只算精修档**：大改不打分
- **暗夜模式**（第 9 条）：导航栏右侧那颗按钮切换，**默认跟随系统**，手动切过之后记住你的选择
  （`settings.theme`）。配色换的是一套**令牌**（`styles.css` 的 `:root[data-theme='dark']`），
  组件一行都不用改；`index.html` 里有一段极早的内联脚本**在首屏绘制之前**定好主题，免得深色用户先闪一下白屏。
  按钮顺序是「暗夜 · 设置」——**设置在最右**。
  ⚠️ 第 13 轮把顶栏另一组元素（原来挂在右上角的那枚警告芯片）**整枚删掉**之后，
  这一对就**自动成了最右边**：`.topbar-right` 本来就是 `margin-left: auto`，
  因此用户那句"将暗夜模式和设置按钮挪到右边"不需要改任何布局
- **导航栏那三枚小标签删掉了**（第 9 条）：方向（中译英/英译中）、文体（新闻编译）、话题（社会）
  都不再显示——方向在原文标题栏里有开关，文体与话题对做题没有指导意义。
  ⚠️ 最后剩的那一枚「内置示例批改」**第 13 轮也删掉了**（用户要求"去掉右上角内置示例批改标志"）。
  它当初留着是为了警告"示例批改不是真的批改"，而它出现的场合之一是**术语判分**
  （那时术语判分借用了 `fixture` 这个来源），于是用户每次批改术语都会看到它跳一下。
  现在术语判分有自己的来源 `local`（见 `domain/types.ts` 的 `JudgeSource`），
  那句提醒改在**结果栏**里说（`AnswerPane` 的「示例批改」芯片），只在真的拿内置示例当结果时出现
- **提交被拦下时用吐司说原因**（第 3 条）：底部居中一条，几秒后自己消失，旁边一颗 × 可以立刻收起。
  它与右下角那条「批改完成通知」是两回事：那条要等人点，这条看完就走
- **切换页面不丢东西**：每道题的作答、逐页批改结果、翻到第几页都**按题目分别保存**，
  切到别的题型再切回来还是离开时的样子；「练习记录」也一直留着
  - **切栏不换题**：点回某一栏时回到**离开时那一篇**（按栏记住"上次看的是哪道题"），
    而不是该栏的第一道内置题。文章栏由文章库供题（`art-…`），内置题库里也有两道文章题，
    原先切回来会落到**另一篇**上，看起来就像"批改被清掉了"（实际那份批改一直留着）
  - **下次打开落到上次关掉时的界面**（`components/last-view.ts`，存 localStorage，见 ADR 0013）：
    **栏 + 题 + 来源 + 页**四样一起记。"来源"必须一起记——文章栏、内置题库、AI 出题、
    自己贴的题共用同一个题号空间，只按题号取回来可能取到**另一来源的同号题**；
    取不回来（那道题已经不在了）就当没记过，退回默认落点。
    记录页与收藏页**不记**：那是去看别的东西，不是练习位置。
    ⚠️ 接回记录/草稿会顺手把会话建起来，因此**起始页号必须在那一处给对**，
    否则"打开就落在上次那一页"会在被接回的瞬间被抹成第 1 页（实测踩过，见 ADR 0019）
  - **方向与原文配套**：文章栏那两个下拉显示的永远是**当前这一篇**所在的格子
    （进入文章题的路子有七八个，与其在每个入口各对齐一遍，不如在渲染时统一对齐）。
    上次选了「中译英」，下次打开就落在中文那一片材料上
- 点「返回编辑」回到作答状态，文字还在

## 批注系统

五种改法，每种错误同时带「改法类型」和「错误分类」（一条错误只归一类，按固定优先级判定）：

| 改法类型 | 视觉表现 |
| --- | --- |
| 替换 | 原词**勾上荧光笔底色**，上方小字写正确写法 |
| 插入 | 在正确位置放一块**荧光笔空位**（宽度正好等于要补进去的内容），补什么写在它上方的方框里 |
| 删除 | 多余内容**勾上荧光笔底色 + 一道横线划掉**（"多出一个词"也走这里，不写任何东西） |
| 整句重写 | 整句勾上底色，上方给出正确整句 |
| 语序调换 | 两段带编号的配对弧线 |

**红橙绿三色共用同一套语言——相应颜色的荧光笔底色**（红＝硬性错误、橙＝表达问题、绿＝表达优秀）。
绿色亮点以前是波浪线，现在也只留底色，三种颜色于是读起来是整齐的一类。
底色挂在被改内容的最外层元素上，因此补写的字更长、两边被撑开时（见下「批注不改变译文本身」），
底色会跟着一起撑成等宽的带子。
**只有"纯删除"额外加一道横线**（用户要求）：删掉的地方不补任何东西，光有底色看不出"是不要了
还是只是被标了一下"。替换与整句重写不加横线——上方会写正确写法，再加横线会和方框、箭头搅在一起。

**收藏里的"整句"按 `,` `。` `"` 来分**（用户要求）：**逗号也算一句的结尾**，
所以存进收藏的是"这一处所在的**那一小截**"，而不是语法意义上的完整句子——
一整句里只有半截被改过时，把整句抄进去反而让人找不到重点。
中文的顿号、分号同理。切出来的各段**首尾相接、不重不漏**（有断言守着）。

**这一段分句的判据只有一份**（`domain/sentences.ts`），对照视图与句子题的「换一句」用的是同一个实现，
差别只有"逗号算不算句末"这一个开关。原先它有两份，**引号那条规则只补进了收藏那份**——
于是 `xxxxx  "xxxx."someone says` 在收藏里切成两句、在对照视图里还是一句（用户报过两次）。
现在有断言逐字比对两边的切点必须完全一致。

**句号后面漏了空格也算断句**（用户第三次报的同一件事）：他的译文里
`…livelihood.The meeting…`、`…villages.Meanwhile,…` 两个句号都粘在下一句首字母上，
断句那一层把三句读成了一句，对照视图因此只有一行。判据是"点号后面紧跟**大写字母**、
而点号前面是**小写字母或数字**"——句首大写是最强的收句信号，而"前面是小写"这一条
把 `U.S.A` 这类缩写排除在外（`3.5` 则靠"点号后面是数字"排除）。
这与提示词同一个口径：提示词里写着"逗号、句号后面与下一个词之间有没有空格，一律忽略"
（AI 不许因为少一个空格扣分），程序自己也不该因为少一个空格少切两刀。
已知取舍：`Mr.Smith`、`e.g.The` 会被切开，与 `e.g.` 后面跟空格时会被切开是同一类
——引一张缩写表不值得，多切一刀只是少给一点上下文。

**标色遵循最小匹配**：修改前后**相同的前缀、相同的后缀、中间相同的那一段都不标色**，
也不算进"修改后的内容"里。`the farmer → the farmers` 只标 `farmer → farmers`；
`a big problem → a huge problem` 只标 `big → huge`；
`people and nature coexist → people with nature coexisting` 只标 `and → with`（中间那段不动）。

这条由 `domain/minimal.ts` 保证，判据有三重：字符级差异 + **词的边界**（不让出半个词）+
"把这些最小改动应用回去能不能还原成新文字"。
其中**先剥掉共同的前缀与后缀**是成败关键：像下面这条真实批注，中间词序被打乱之后
字符级对齐会支离破碎、判据不合格而退回"整段替换"，连带把共同的两头一起标红——
剥掉共同的两头（退到词边界上，避免把 `farmer → farmers` 剥成"只差一个 s"）之后才切得对：

```
原译 the Director-general of Statistics Department of Comprehensive National Economy
改法 the Director General of the Department of Comprehensive Statistics of the National Economy
→ 只标中间那段；「the Director」与「National Economy」都不标
```

`scripts/smoke.ts` 里有五组用例钉着这些形状（含上面这条真实案例）。

注意：**画成哪一种，不一定等于 AI 说的 type**。AI 常把"漏了一个词"报成 `replace`，
按单词求最小不同项之后它其实是 `insert`（一个字都不勾）；把"多了一个词"报成 `replace` 时，
实际画出来是 `delete`。徽标、气泡、右下角说明都跟着**画出来的样子**走（见 `renderedKindOf`）。
（README 里说的"勾掉／勾出"就是页面上那条底色。）

五种改法都可以**点**：点中后那一行下面浮出气泡（**完整**说明，不截断），右下角同时换成这一处的完整解释。
要点是弧线的配对键：`data-reorder-index` 存的是**片段序号**（`sourceIndex`），不是片段在译文里的字符位置——
两者必须分开，写成字符位置的话弧线会一条都配不出来，而页面上照样渲染 `<svg>`，光看"有没有弧线元素"是发现不了的。

分类里最容易出错的是"语法错误被写成用词不当"：**语法不成立（时态、语态、主谓一致、词形、
句子结构）一律归语法类，红色**；`用词不当` 只留给"语法正确但用词不准确、不地道"的情况。
术语更严：**固定表述只要错一个词**（拼写、大小写、漏词、自造近义说法）就算术语不准，红色。
这几条都写进了提示词，并且在交卷自查清单里再问一遍。

颜色分级由**错误分类推导**，不由 AI 选——这样同一类错误每次都是同一个颜色：

- **红 = 硬性错误**：术语不准、**语法**、冠词/介词/单复数、标点，以及**漏得多/多得多**的漏译增译
- **橙 = 表达问题**：语序错、搭配不当、用词不当、语体不符，以及**漏得少/多得多**的漏译增译
- **绿 = 表达优秀**（亮点单独记录，不计入扣分；同样是荧光笔底色，只是颜色是绿的，不再加波浪线）

**漏译与增译的轻重由程序按字数判**（用户口径："漏 0~2 个单位算橙色，漏三个单位以上算红色"）：
单位取**译文那一侧**的口径（中译英数词、英译中数汉字），与文章篇幅的口径恰好相反，见 `domain/severity.ts`。
因此这两类**同类不同色**：颜色的唯一来源是解析时算好的 `ValidatedError.hard`，
下游一律读它、不按分类现推（否则会出现"标着橙却按红扣了 8 分"）。详见 **ADR 0014**。

修改风格两档，**产物不同**（见 **ADR 0015**）：

| | 精修 | 大改 |
| --- | --- | --- |
| 做法 | 逐处批注：指出要改哪段、改成什么、为什么 | **整篇逐句重写**，每句说明为什么这么改，并给出这一句对应的原文 |
| 分数 | 程序按错误列表算 | **不打分、也不给总评**（用户拍板，见 ADR 0020） |
| 视图 | 批改视图 / 对照视图都行 | **只能看对照**（开关保留但禁用并注明原因） |
| 版式 | 四格 2×2 | **左栏上下（原文 / 大改档那一句说明）＋ 右栏整栏对照**，右下角那一栏整块不画（第 4 条） |
| 统计 | 有错误归类 | 不逐处批改、不统计 |

> ⚠️ **两档的名字换过一次**（用户要求："将'润色'改成'精修'，'精修'改成'大改'"）：
> 界面上的「精修」原先叫"润色"，「大改」原先叫"精修"。**内部键一律没动**
> （`polish` / `refine` 已经写进了解析、提示词与落盘的练习记录），因此读代码时以键为准——
> `refine*` 在代码里指的都是**大改**，不是界面上那个「精修」。

> ⚠️ **大改档原先有一处"有意偏离"**（AI 给分而不是程序算分，0–100 两档不可比）——
> 用户后来把它**整个取消了**："分数打分只有精修部分有"，连总评文字也去掉（**ADR 0020**）。
> 因此现在全站只有一个分数口径，那句"两档不可比"的提醒也就不需要了。

**每条说明的每一小点用「；」隔开**（用户要求）：界面上那张小卡片本来就是按分号断行、
逐条编号的（①②③），而模型爱把"哪里错了"和"正确写法是什么"用逗号串成一句——
卡片一窄就成了读不到头的流水句。因此提示词里把这条写死：「一个分号 = 一个小点」，
并且交卷自查清单里再问一遍。
**大改档的逐句解释也是同一套排版**（用户要求："大改模式下，解释部分也要按照分号进行
圈一圈二的序号标注分行"）：判据只有一份，在 `components/explain-lines.tsx`，
小卡片与大改的解释都调它——不这么收着，同一段说明就会在小卡片里是①②③、在大改那边又是一整段。

## 翻译标准

提示词里写死的判断依据，只有四条，按重要性排序：

1. **术语与固定表述必须一字不差**——这是唯一的硬性红线
2. **不得造成误解**
3. **不得漏译**（含原文的逻辑关系）
4. **意思传达准确即可**——句式可重组、语序可调整、词语可换同义表达

也就是说：**表达方式允许合理意译，信息与术语不允许走样**。译文用词与常见译法不同但意思到位，不算错误。

**参考译文不发给模型**：它只存在本地、只给人看（原文栏里那份可折叠的）。发给模型有两个坏处：
模型会退化成"逐字对照标准答案"，与"允许合理意译"直接打架；题目数据里的"标准答案"
也没必要离开这台机器。因此批改的输入只有**原文 + 你的译文**，术语准不准靠模型自身的知识判断。

## 分数

**只有精修档有分数**，而且 **AI 不打分**——分数由程序按标出来的错误算（`src/domain/scoring.ts`）：

```
满分 100
  漏译：按字数扣（漏多少字/词就扣多少分，不再另扣固定分）
  其它硬性错误（红）：每处 8 分
  表达问题（橙）：每处 3 分
  下限 0；亮点不加分
```

这样分数与页面上实际标出的错误**永远一致**，也不会出现"90 分却标了 8 处硬性错误"这种自相矛盾；
用户还能自己核出这个分数是怎么来的。三处细节：

- **漏译的扣分单独写一行**：「漏译扣 X 分（漏 N 个单位）」（见 ADR 0021）。
  漏译的**颜色**仍按老口径判（漏 1–2 个单位橙、3 个及以上红）——颜色与扣分是两件事。
- **图例里的处数把漏译算在内**（它也会标色），只是它不参与"每处固定分"。
- **算分要用到翻译方向**——漏译的单位是按译文那一侧数的（中译英数词、英译中数汉字），
  不拿方向就分不出该数词还是数汉字。
- **多译仍按每处扣**（用户只点名了漏译）：真要与漏译对齐，见 `scoring.ts` 里那处开关说明。

**大改档不打分、也没有总评**（用户拍板，见 ADR 0020）：那一档不逐处批改、没有错误可扣，
原先是让 AI 给一个"整篇印象分"，与精修档的分数含义不同又不能直接比。现在干脆取消了它——
左下角只写一句"大改档不打分"，「批改记录」与练习记录里那一次也标「不打分」。
于是**全站只剩一种分数口径**：精修档那个"程序按错误算、你能自己核"的分数。

## 内容来源

- **文章库**（成篇练习材料，自带参考译文）：预置材料，按「领域 × 方向」组织，**只进「文章」栏**。
  **五个板块 × 两个方向，各 5 篇（生态英译中 3 篇），共 48 篇**。
  材料来自仓库根目录的 **`文章.docx`**——一份「外研社·国才杯」笔译练习材料，共五组，
  每组五个板块（社会／经济／文化／生态／科技）、每个板块中译英与英译中各一篇，每篇都配参考译文。
  **存的是整篇正文，不是选段**——用户要的是"逐段翻译整篇文章"；赛制篇幅（英译汉 250–350 词、
  汉译英 200–300 字）是**考场**的要求，练习时不拿它截断，也不再据此在卡片上标"符合/不符合"。
  练习时按 **一页 = 一个自然段**切开（不足 50 单位的自然段与相邻段合并；英译中计词、中译英计汉字），
  实测 48 篇共 222 个自然段、切出 201 页，其中 90% 就是一个自然段。卡片上显示**全文篇幅 + 共几页**。
  **参考译文随材料一起入库**，与原文**逐段对齐**（段数必须相等，逐篇验），
  因此正文末尾那个「参考译文」折叠块铺出来的就是这一页的译文；它也让同一篇每次练到的标准答案都一样
  （ADR 0003：参考译文是**题目数据的一部分**，不随每次批改漂移）。
  正文由 `node scripts/build-articles.mjs --write` 从 docx 现算（产物写到 `src/domain/articles-data/`
  两个生成文件，改内容请改 docx）；`node scripts/check-articles.mjs` 是这套数据的验收口
  （篇数、完整性、不雷同、译文对齐、分页体检）。
  为什么**没有来源媒体与 URL**、题号为什么带方向、生态那格为什么只有 3 篇——见 **ADR 0011**。
- **官方原文**：人工核对过来源的文本（内置示例题）
- **术语库**（第 13 轮整批换过）：两个**术语范围**——**国内机关名称** 83 条（17 页）、
  **国际机关名称** 60 条（12 页），源材料是仓库根目录的 `国内机构.docx` 与 `国际机构.docx`，
  正文由 `node scripts/build-terms.mjs --write` 从 docx 现算（产物写到 `src/domain/terms-data/`
  两个生成文件，改内容请改 docx）；`node scripts/check-terms.mjs` 是这套数据的验收口
  （条数、分页、别名与拼写变体、**"任何一条不能接受别的术语的答案"**）。
  术语题的批改**完全本地对照官方译名**，不交给 AI——这是 `scoring.ts`「分数由程序算」
  那条原则在术语上的落地；判分口径与"为什么放宽到接受官方别名"见 **ADR 0024**，
  为什么术语栏另开一张与话题领域平行的表见 **ADR 0025**。
  > 换代之前那 43 条按话题领域分组的政论术语（"高质量发展""共同富裕"……）
  > 连同 9 条古文名句一起**归档**在 `src/domain/legacy-terms.ts`——
  > 数据留在仓库里、**不被任何代码引用**（见 ADR 0023）。那批词里的 26 条
  > （政治建设／教育强国／国际传播三组）在更早那次领域收敛时已经删掉，只能翻 git 历史。
- **AI 生成**：按领域与文体生成（界面上的「AI 出题」），程序硬校验字数（英按词数、中按字符数），越界自动重新生成
- 话题预设：**就是文章库那五个板块**（社会／经济／文化／生态／科技），与领域下拉逐个对齐
  （冒烟测试里有断言盯着"两处口径必须一致"），也可自行输入
  ⚠️ **术语栏不在这个话题预设里**：它用自己的两个**术语范围**，且「AI 出题」在术语栏没有入口

> ⚠️ DeepSeek 模型不具备联网能力，不能"去抓一篇新华社文章"——那样只会编造出不存在的文章。AI 生成的内容只保证**文体、话题、字数**符合比赛要求，不代表真实新闻。
>
> ⚠️ **版权与出处**：文章库现在的正文来自 `文章.docx`——那是为练习**编写**的材料（围绕 2026 年事件），
> 不是某家媒体的原文，因此**不填来源媒体、也不填 URL**：编不出真实出处就不要编。
> 早先那版文章库收的是中国官方媒体（新华英文／中国日报／CGTN／人民网英文／gov.cn／china.org.cn，
> 中文侧是新华社／人民日报／中国政府网／光明网／求是网／教育部）的**真实新闻全文并保留真实 URL**，
> 那批数据与两个抓取脚本已按用户要求删掉（git 历史里还在）。
> **不要**把商业媒体（路透、BBC、卫报、AP）的正文加进公开仓库再附参考译文——那是实打实的版权风险。
> 收录口径的由来见 ADR 0007 与 ADR 0009，本轮为什么换掉整套口径见 **ADR 0010**（领域）与 **ADR 0011**（材料）。

## 技术架构

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 运行时 | Cloudflare Pages + Pages Functions | 前端是静态产物；三个接口由 `functions/` 承担。本地那份由 Vite 开发插件承担，**同一份实现**（见 ADR 0026） |
| 前端 | React + TypeScript + Vite | |
| 数据库 | Cloudflare D1（**尚未开始**） | 题库、术语表、每次提交记录（含答题原文与完整批改）；现在都存在浏览器 localStorage 里 |
| AI | DeepSeek API（默认 `deepseek-flash`） | 开启 JSON 输出模式，返回结构化数据 |
| 密钥 | 本地 `.dev.vars` / 线上 Cloudflare Secrets | 均不入库 |
| 访问控制 | **无**（对所有人开放） | 2026-09-25 按用户要求拆掉口令门："任何人都可以访问"（见 ADR 0027）——谁拿到网址谁就能批改、花余额 |

### 批改请求的走向

浏览器代码里**永远不出现 API 密钥**。提交批改时请求发给同源的 `/api/judge`：

```
浏览器 → /api/judge（只带原文与你的译文）→ 补上密钥 → api.deepseek.com → 解析校验 → 返回结构化批改
```

本地开发时 `/api/judge` 由 `vite-plugin-judge-api.ts` 提供（只从 `.dev.vars` 读密钥）；
线上由 `functions/api/judge.ts` 提供（密钥来自 Cloudflare 的 Secret），前端代码无需改动。
两边的**业务逻辑是同一份**（`src/server/api.ts`），宿主只负责"密钥从哪来、日志往哪写、
失败记录往哪存"——理由见 **ADR 0026**：写两份实现一旦漂移，故障只在线上出现、本地复现不了。

AI 返回的结果要经过 `src/domain/parse.ts` 的解析与 `src/domain/validate.ts` 的位置校验：
不合格时把**具体原因**回传给模型重试（最多 3 次），仍不合格就如实报错并保留你的输入。
单次批改实测约 10–35 秒（重试会明显变慢）。

返回给前端的除了结构化的批改结果，还有 `raw`——**模型原样返回的文本**（多段批改按段拼好）。
界面上的「点击查看 AI 完整返回内容」显示的就是它：想知道"模型到底说了什么、被程序改成了什么"，
不必去翻失败存档（那里只有失败的那几次）。内置示例没有"模型原文"这回事，
就用 `toAiShape()` 把解析后的批注还原成 AI 的字段形状，如实标出它是示例。

### 批改一次要花多少钱（实测）

先用**官方 tokenizer** 把提示词数成 token（免费、精确），再用**真实 API** 跑一次、读返回里的
`usage`（这是唯一能拿到"输出到底多少 token"的办法，一次约几分钱）。
两个工具都在 `scripts/measure-cost.mjs`（`--live` 才会真发请求，`--thinking=disabled` 量关掉思考的样子）。

单价按 [DeepSeek 官方价格页](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)（`deepseek-flash`，元/百万 tokens；
高峰＝北京时间周一至周五 9:00–12:00、14:00–18:00，其余时段含周末与节假日为**空闲时段，正好半价**）：

| | 空闲时段 | 高峰时段 |
| --- | --- | --- |
| 输入（缓存命中） | 0.02 | 0.04 |
| 输入（缓存未命中） | 1 | 2 |
| 输出 | 4 | 8 |

提示词实测（句子题 519 字符用户消息）：**系统提示 3465 tokens**、用户消息 191 tokens，
一次请求输入合计约 **3700 tokens**。系统提示每道题一个字都不变，所以几乎总是**缓存命中**
（真实调用里 3708 个输入 token 有 3328 个命中缓存）——输入便宜到可以忽略。

**钱几乎全花在"想"上**：真实调用一次句子题（`deepseek-flash`）：

| | 输入 | 输出 | 其中思考 | 空闲时段花费 | 高峰时段花费 |
| --- | --- | --- | --- | --- | --- |
| 默认（思考打开，effort=high） | 3708（3328 命中缓存 + 380 未命中） | **8480** | 8187 | **约 0.034 元** | **约 0.069 元** |
| 关掉思考（`thinking: {type:"disabled"}`） | 3683（全未命中） | **399** | 0 | **约 0.005 元** | **约 0.011 元** |

也就是：**默认设置下一次句子题批改约 3–7 分钱**。这笔钱里 **98.7% 是输出 token 的费用**，
而输出里 8187/8480 都是思考（写出来的 JSON 只有约 300 tokens）。按题型粗算
（下表只有句子那一行是**量出来的**，其余是按输入比例与"思考量随题量增长"推的区间）：

| 题型 | 一次请求数 | 空闲时段 | 高峰时段 |
| --- | --- | --- | --- |
| 术语 | 1 | 1–3 分 | 2–7 分 |
| 句子 | 1 | **3.4 分（实测）** | **6.9 分（实测）** |
| 段落 | 1 | 3–5 分 | 7–10 分 |
| 文章（4 段并行，每段都带整篇原文） | 4 | 14–22 分 | 28–44 分 |

按这个数估一下日常开销（空闲时段、思考打开）：一天一道句子题 + 一天一篇作文 ≈ 0.17 元/天，
一年 ≈ 60–70 元；全放到高峰时段则翻倍。要精确值就把 `measure-cost.mjs --live` 对着那类题各跑一次。

省钱的三个办法，按性价比排：

1. **关掉思考模式**（`{"thinking": {"type": "disabled"}}`）：实测输出 token 从 8480 掉到 399，
   费用约为原来的 **1/6.5**，而且返回的内容**照样通过了 6 处批注的解析与位置校验**（同一次实测里验的）。
   代价是"数错位置"这类毛病会更常见——那正是思考最管用的地方，建议先小样本对比再决定。
2. **把强度降到 `reasoning_effort: "low"`**：介于两者之间（未实测）。
3. 顺带一提：**思考模式下 `temperature` 是无效参数**（官方文档明说思考模式不支持 temperature /
   presence_penalty / frequency_penalty，传了不报错也不生效），所以现在代码里那个 `temperature: 0.2`
   在默认设置下并没有起过作用；关掉思考之后它才会生效。

## 本地开发

### 一键启动（推荐）

| 系统 | 怎么做 |
| --- | --- |
| Windows | **双击项目根目录的 `启动.bat`** |
| macOS / Linux | 终端执行 `./start.sh` |

脚本会依次检查 Node 版本、依赖是否装好、`.dev.vars` 里有没有密钥、端口是否被占用，
然后启动服务并在就绪后自动打开浏览器。**出现任何问题它都会在结尾停下等你按键**，
不会一闪而过；诊断时可以先跑 `启动.bat -Diagnose`（只做检查、不启动服务）。

可选参数：

```
启动.bat -NoBrowser        # 只启动服务，不打开浏览器
启动.bat -Port 5200        # 换端口（默认 5180）
启动.bat -Diagnose         # 只做环境检查
./start.sh --no-browser
./start.sh --port 5200
```

> ⚠️ **维护者注意**：`启动.bat` 与 `start.ps1` 必须保持**纯 ASCII**，不要往里加中文。
> cmd.exe 用系统代码页（中文 Windows 是 GBK）读取 `.bat`，而 Windows PowerShell 5.1
> 会把无 BOM 的 `.ps1` 当 ANSI 读；一旦文件里出现非 ASCII 字符，字符串字面量会被解码错乱、
> 脚本语法报错，用户看到的现象是"双击后窗口闪退"，极难排查。
> 中文提示属于网页界面，控制台输出保持英文。`npm run smoke` 里有护栏会拦住这个错误。

### 手动启动

```bash
npm install
cp .dev.vars.example .dev.vars   # Windows: copy .dev.vars.example .dev.vars
npm run dev                      # http://127.0.0.1:5180/
```

`.dev.vars` 需要填 `DEEPSEEK_API_KEY`（必需）。另外两行 `ACCESS_PASSWORD` 与 `SESSION_SECRET`
是访问口令门用的键，那道门已按用户要求拆掉（见 ADR 0027），服务端不再读它们。
没有密钥也能启动，但提交批改会告诉你密钥缺失；此时可以点内置示例看批注效果。

其他命令：

| 命令 | 作用 |
| --- | --- |
| `npm run smoke` | 冒烟测试：批注位置校验、排版计算、失败存档、界面渲染，并生成界面截屏（没有 Edge/Chrome 时这一项**跳过**，不算失败，摘要里会报"另有 N 项因环境不具备而跳过"） |
| `node scripts/shots.mjs` | 只生成界面截屏（与上面同一套代码） |
| `node scripts/probe-fix-align.mjs [题号] [题型] [第几题] [前缀] [拖动像素]` | 在真实浏览器里**量**「补写的字」与它正下方那行荧光笔带子的左右偏差、宽度差与上下余量（拖一次分隔条再量一遍），按像素数「空档里有没有别人的字」，并且点一遍勾画看气泡出不出来。对齐这件事只有量坐标才说得清。**发现越出带子或明显不居中、或一处都量不到时，退出码为 1**（可直接进 CI）。`--synthetic` 换成"被改内容跨两行"的构造题、`--insert` 换成"漏了一个词"（量插入空位），`--pad-effect` 把空档摘掉再量一遍（看撑宽把原文推到了哪儿），`--tight` 把行距拉到最紧再量一遍，`--ink-negative` 把旧的画法（负 margin）放回来给这把尺子做对照，`--records` 量练习记录页那三条分隔条 |
| `node scripts/measure-cost.mjs [--live] [--thinking=disabled]` | 量一次批改花多少 token：先用官方 tokenizer 数提示词（免费），`--live` 再真发一次请求读 `usage`（几分钱） |
| `node scripts/live-judge.mjs [1-3]` | 用真实 API 跑一次句子题批改并打印细节，用于核对提示词与解析器 |
| `node scripts/live-refine.mjs [1-2]` | 用真实 API 跑一次**大改档**（整篇逐句重写 + 总评）并打印逐句结果与对照行。大改的提示词与解析器都是另起的一套，只有真调用才证明得了"模型真的按这个格式返回" |
| `node scripts/live-sections.mjs` | 用真实 API 验证文章题的按段并行与序号换算 |
| `node scripts/verify-per-page.mjs [地址]` | 对着**已在运行**的服务用真实浏览器走一遍**逐页批改**（接口用桩并记下每一次请求）：**翻页不提交且草稿留着**、**点两张卡片的正文都不会把它们关掉**、**小卡片中心命中的就是卡片本身**、提交后按钮变"批改中…"且作答框只读、等待窗两个按钮、**批改没回来就能翻到下一页**、此时别处也提交不了、批完右下角通知可点回那一页、每页各发一次、不串页、翻回已批过的页不重提、**每次请求的答案起点都是 0**；第 2／3／5／10／11／12 条那一批也在这里验：**提交门四步**（太短拦下且一个请求都不发／够长放行／一字不差拦下／改一句又放行）、**删记录**（叉号 → 确认 → 练习记录与下拉同步少一条）、**分割线位置记住**（拖过→刷新还在原处；双击恢复默认→刷新仍是自动）、**记住正在看第几次**（翻页往返 + 刷新都还是那一次）、**最高分标签批过之后变成真实分数**、**刷新后编辑态与草稿仍在（而旧结果不再挂在页上）**；第 4 条那一批也在这里量**真实矩形**：**大改档的版式**（左栏上原文、下总评，右栏整栏都是对照，右下角那一栏 `display: none`）、三行标签是「原文 / 我的译文 / 修改译文」、每组四块、**左下角只写"大改档不打分"且一个数字都没有**。顺带验小卡片那颗「收藏」**真的收得到点击**（看计算出的 `pointer-events` + 一次真实命中测试），以及收藏页每条都贴出"这一处所在的那一段原文"。**任一条不过退出码为 1**（109 项） |
| `node scripts/verify-live-server.mjs [地址] [题号]` | 对着已在运行的服务跑一次提交，确认**不白屏**（开发服务器缓存陈旧是本站踩过的坑）；随后进练习记录页确认**「当时的译文」也能切视图**，以及**原文栏给的是当时那一段**（文章题标着"第 N 页"） |
| `node scripts/verify-article-bar.mjs [地址]` | **领域与方向现在长在原文标题栏里**（领域紧挨「原文」三个字；顶部那条横条已删）／选文章弹窗（卡片是"事件锚点当标题 + 篇幅 + 共几页 + **最高分 N 分**"、**没有来源媒体**）、**正文末尾的「参考译文」折叠块**（点开铺出这一页译文 → 再点折回；没有译文的题连这四个字都不出现）、**顶栏不再挂方向/文体/话题三枚标签**、**暗夜模式**（按钮顺序「暗夜 · 设置」、点一下换主题、存下来、刷新不回弹、底色真的跟着换）、**续做与记忆**（没记住页号时从没批完的那一段继续；记住了页号时回到那一页、那一栏）、练完的不再主动打开、列表里排到最后并标「已完成」（75 项） |
| `node scripts/build-articles.mjs [--write]` | **文章库正文的唯一生成入口**：从仓库根目录的 `文章.docx` 现算 48 篇（订正英文撇号、丢掉重复的两篇猕猴），不带 `--write` 时只体检不写文件 |
| `node scripts/check-articles.mjs [--brief]` | **文章库体检**：每格篇数（社会/经济/文化/科技 各 5+5、生态 3+5 = 48 篇）、每篇完整（题号形状、事件锚点、没有来源/URL 字段、正文与译文段数相等、units 与实测一致）、同一格不雷同（8 字片段相似度 > 0.5 判失败；"同题不同文"只提醒）、**分页与规则的手写实现逐页一致**、每一页的译文逐段对上、拼起来一字不丢、**原文与译文里没有"句号漏空格"的连写**，并报出页数分布（74 项） |
| `node scripts/build-terms.mjs [--write]` | **术语库正文的唯一生成入口**：从仓库根目录的 `国内机构.docx` 与 `国际机构.docx` 现算成 143 条（国内机关名称 83 条/17 页、国际机关名称 60 条/12 页），顺手订正英文撇号、把官方缩写与英美拼写变体写进 `enAlt`、把"一英多中"互相登记进 `zhAlt`；不带 `--write` 时只体检不写文件（第 13 轮新增） |
| `node scripts/check-terms.mjs` | **术语库体检**：两个范围的条数与页数、每页 1–5 条与末页余数、题号换代（`term-v3-…`）与旧代次解析失败、**每一条的答案都不能被别的术语接受**（盯"放宽过头"这类不报错的 bug）、变体与别名确实算对、题干按页切分逐页对上（170 项） |
| `node scripts/verify-term-mode.mjs [地址]` | 术语栏（第 13 轮重写）：**范围弹窗两屏**（两张卡片上的条数页数、点开是一张 83/60 行的中英对照平表、「返回」退回、「确定」切过去并落到第 1 页）、**原文标题栏一颗按钮都没有**（「换一换」「AI 出题」「重新作答」「重新贴一篇」全撤掉）、**17 页翻页**（页脚那句"第 N / 17 页 · 待批改"、首尾两页的按钮禁用、翻走再回来草稿还在、翻页一次请求都不发）、**末页只有三条**（画五行、三条可填、另两行禁用）、**本地判分**（对错记号、译错的划掉并在同一行给出官方译名、判完只读、**请求数为 0**）、**「返回编辑」后五条各回各框**（用户报的那个 bug）、方向切换与「／」那条一英多中、**顶栏没有「内置示例批改」芯片**（109 项） |
| `node scripts/verify-sentence-mode.mjs [地址]` | 句子栏：只给领域、不给选文章与方向（15 项） |
| `node scripts/probe-section-offset.mjs [地址]` | 一次性探针：验服务端返回的批注位置**是不是按 `answerSections[0].start` 平移**（会真发两次批改，几厘钱）。改了逐页提交的坐标口径时跑一次 |
| `node scripts/dump-prompt.mjs --out` | 导出送给 AI 的完整提示词到 `docs/prompt-system.txt`（批改 + AI 出题两套） |
| `node scripts/dump-prompt.mjs --generation` | 只打印 **AI 出题**那两段提示词 |
| `node scripts/failures.mjs` | 查看 AI 失败存档（见下） |
| `npm run typecheck` | 类型检查（**两份配置**：`tsconfig.json` 管 src 与本地接口，`tsconfig.scripts.json` 管 scripts/） |
| `npm run typecheck:src` / `npm run typecheck:scripts` | 只检查其中一侧 |
| `npm run build` | 类型检查 + 生产构建 |

> 关于整站唯一的探针：`probe-fix-align.mjs` 是"补写内容与荧光带对齐"这个高风险特性的**唯一证据**。
> 它现在会在发现问题时以退出码 1 结束——在此之前它全文没有一处 `process.exitCode`，
> 而 `report()` 里的 `?? 0` 兜底会让"一处都没量到"与"量了、全部合格"打印出**一模一样**的
> "越出或明显不居中的有 0 处"，所以它既不能进 CI，也不能当验收依据。已改正。

### 提示词

提示词是 AI 层的唯一真相来源，全部在 `src/domain/prompt.ts` 里。想直接读或改，
先跑 `node scripts/dump-prompt.mjs --out`：它会输出**模型真正收到的那份文本**
（含拼接出来的分类表与五种改法的完整 JSON 示例）到 `docs/prompt-system.txt`。

提示词有**三套**，都在 `src/domain/prompt.ts` 里：批改（系统提示 + 用户消息 + 重试消息）、
大改（`buildRefineSystemPrompt` / `buildRefineUserPrompt` / `buildRefineRetryPrompt`，
产物完全不同——整篇逐句重写 + 每句对应的原文 + 逐句解释、**不打分**，见 ADR 0015 与 0020）、AI 出题（`--generation` 只打印这一套）。

### 批改的方法：分遍自查，每遍专攻一类（用户指定）

不要一次把整段看一遍就出结果。提示词里要求模型**按顺序走几遍**，每遍只找那一类的错。
⚠️ **两个方向的遍数与顺序不一样**（用户第 14 条的口径：只给英译中换新顺序）：

**英译中·精修——五遍**（顺序就是用户点名的那四件事，语法与标点排最后但**仍然要查**）：

| 第几遍 | 只看什么 | 用哪些分类 | 颜色 |
| --- | --- | --- | --- |
| 1 | **漏译与多译**：原文有的信息译文里有没有；译文里有没有原文没有的 | omission / addition | 由程序按字数定 |
| 2 | **专有名词与术语**是否与官方译法一字不差 | terminology | 红 |
| 3 | **啰嗦**：绕圈子、重复、可有可无的堆砌（删掉更利落的地方） | verbosity | 橙 |
| 4 | **更好的表达、更好的用词**：更贴切更地道的说法（搭配、语序、语体） | word-choice / collocation / register / word-order | 橙 |
| 5 | **语法与标点**：冠词、时态、单复数、介词、主谓一致、句子结构；中文译文是否用全角标点 | grammar / function-word / punctuation | 红 |

**中译英·精修——四遍**（没变）：语法与标点 → 漏译与多译 → 术语 → 表达是否地道（含明显啰嗦）。

**亮点**在走完这几遍之后单独给一遍（那几遍都在挑错，正面的判断放最后一起说）。
⚠️ **"检查顺序"与"归类优先级"是两件事**（新版提示词里分开写了）：英译中的检查顺序把语法排到最后，
但**归类时硬性错误（红）仍然优先于表达问题（橙）**——否则一处"又啰嗦又语法不通"的地方会被归成橙色。
⚠️ 这几遍是**一次请求内**的自查方法，不是几次请求（真发几次会把花费与耗时都乘上几倍）——
因此"它真的走了几遍"这件事，程序验不了，只能靠提示词与模型的自律。详见 **ADR 0014 与 0021**。

出题的那套把**赛制规格与命题风格整块附了进去**——模型不联网，不知道「外研社·国才杯」
是什么比赛、怎么命题；不交代清楚，它就会按通用英语作文的路子出题（题材飘、文体不对、
埋不进术语）。附进去的内容：

- **一句话命题风格**：以「中国时政／国情话语外译 + 通用非文学翻译能力」为底色，
  越往高阶越加入国际传播型任务（术语阐释、译后编辑、新闻编译、文学翻译）——
  要的是"中国话语怎么准确、规范、可传播地译成英文"，**不是"优美的文学翻译"**。
- **主题域固定、不要越界**：**社会／经济／文化／生态／科技**（就是站里那五个板块）
  + 习近平新时代中国特色社会主义思想核心概念；
  明确不写娱乐、体育、个人生活之类（"AI 出题"的领域预设也按这五个排，两处由断言盯着必须一致）。
- **素材与语体**：参照《习近平谈治国理政》《理解当代中国》《核心术语学习手册》、
  党的二十大报告、教育强国纲要；政论与外宣笔调、书面语、长句多、逻辑连接词密。
- **只考的四类文体**，以及"术语是主要考点、要有意识地埋进去"。
- **评分看重**：术语准、政治含义不跑偏、句式符合英文习惯、逻辑不断、不硬译不漏译，
  展开成四条判分尺子。
- "不要偏题怪题"，以及"你**不能联网**，不要编造出处／链接／引语"。

篇幅数字**取自 `generate.ts` 的 `LENGTH_RULE`**，不另抄一份，
免得提示词与硬校验说的不是同一个数。

提示词里有几条来自实际失败的硬性约定，改动时不要删：

- 允许合理意译，但**术语与固定表述必须一字不差**
- **语法错误一律归红色分类**（`grammar` / `function-word`），不许用 `word-choice` 兜底；
  `word-choice` 只留给"语法正确但用词不准"；术语只要错一个词就算 `terminology`
- **漏译一律用 insert、多译一律用 delete**，且只把漏掉/多出的那部分放进 targetText / oldText——
  程序要靠这个形状量字数、才判得出红橙**与扣多少分**（见 ADR 0014 与 0021）
- **每一处批注都要给 `sourceText`（这一处对应的原文），中译英尤其要给**——
  那是"点批注 → 左边原文栏同色标出对应位置"唯一的数据来源（第 1 条，见 ADR 0003 的补充）
- 每种改法都要有**完整的字段示例**——曾经因为只给了 replace 的完整示例，
  模型在 insert 那条上漏写了 `category`，整份返回因此作废
- 数序号必须逐条回查，不能凭记忆写
- 交卷前有一份自查清单：**几遍都走完了吗**、漏译/多译的形状对不对、同一处有没有报两遍、
  以及 id / type / category / explanation 一个都不能少

### AI 失败存档：用来迭代提示词

批改失败时（AI 返回的 JSON 坏掉、位置全部对不上、输出被截断、重试用完仍不合格），
系统会把**每一次失败尝试的原始返回全文**连同这次请求的完整上下文
（原文、你的作答、方向、文体、风格、模型名）写进 `.ai-failures/`：

```
.ai-failures/
  <时间>-<失败类型>.json   单次失败的完整记录
  summary.log              一眼能扫的摘要行
```

原始返回是排查提示词问题的唯一直接证据，所以**完整保留、不做截断**。
存档里绝不会写入 API 密钥（有测试保证这一点）。整个目录已 gitignore。

```bash
node scripts/failures.mjs              # 列出全部失败记录
node scripts/failures.mjs --stats      # 按失败类型统计（看哪类问题最常出现）
node scripts/failures.mjs --raw 1      # 看第 1 条的原始返回全文
```

### 界面截屏

`npm run smoke`（或 `node scripts/shots.mjs`）会在真实浏览器里渲染界面并生成三张图，
放在 `.screenshots/`，**留给你人工确认**（不做自动比对——像素层面的好看与否只有人能判断）：

| 文件 | 内容 |
| --- | --- |
| `01-compose.png` | 桌面尺寸，写译文的状态 |
| `02-result.png` | 桌面尺寸，**真的点了提交之后**的批改结果（句子题），并已点开一处勾画：气泡 + 右下角单处说明 |
| `03-marks.png` | 桌面尺寸，句子题的批改结果——**同时含替换、插入、删除三种标注**，最适合看标注长什么样 |
| `04-mobile.png` | 手机宽度，检查四栏叠放的效果 |

后两张通过点击顶部导航切换到对应题型生成；批改结果是通过在页面里注入接口桩实现的：
把 `/api/judge` 的响应换成本地假批改，所以截屏不花 API 钱，
但走的是与真实批改完全相同的校验与渲染代码。

## 部署

线上是 **Cloudflare Pages + Pages Functions**：前端是静态产物，三个接口由 `functions/` 承担。

1. 推送到 GitHub 仓库（`main` 分支）
2. Cloudflare 控制台 → Workers & Pages → 创建 Pages 项目 → 关联该仓库，构建设置填：

   > 走控制台这条路建出来的项目，Git 推送会自动接好（下文"改了代码怎么同步"那一节）。
   > 本项目线上那个实例是**用 API 建的**，因此还差一步授权，见下面那一节。

   | 项 | 值 |
   | --- | --- |
   | 生产分支 | `main` |
   | 框架预设 | 无 |
   | 构建命令 | `npm run build` |
   | 构建输出目录 | `dist` |
   | 根目录 | 留空 |

   `functions/` 会被自动识别成 Pages Functions，不需要额外配置。构建机上没有 `.dev.vars`，
   也不需要——那个文件只服务本地开发，`vite build` 根本不读它。
3. 设置唯一一个 Secret（控制台的 Settings → Variables and Secrets，或命令行）：

   ```
   npx wrangler pages secret put DEEPSEEK_API_KEY --project-name fanyipigai
   ```

   少了它，批改会返回一句明确的"没有配置 DeepSeek API 密钥"。Secret 是**运行时**注入的，
   配好立刻生效，不必重新部署。

   ⚠️ 站点**没有任何访问控制**（用户要求，见 ADR 0027）：谁拿到网址谁就能批改、花你的余额。
   想加一道闸不必改这个仓库的任何代码——Cloudflare 的 Rate limiting 规则（按 IP 限速），
   或者给 `/api/*` 挂 Turnstile，两种都在 Cloudflare 那一层。
4. D1 数据库（账号体系靠它，见 ADR 0028）：

   ```
   npx wrangler d1 create fanyipigai-db          # 建库，把返回的 database_id 填进 wrangler.toml
   npx wrangler d1 execute fanyipigai-db --remote --file=schema.sql   # 建表（幂等）
   ```

   `wrangler.toml` 里 `[[d1_databases]]` 那段就是绑定：**没有它，注册登录一律 500**。
   部署时用 `npm run deploy`（它会把绑定一起带上）。
5. 把第一个管理员指定出来（**管理权限没有自助入口**，注册接口一律写 0）：

   ```
   npx wrangler d1 execute fanyipigai-db --remote \
     --command "UPDATE users SET is_admin = 1 WHERE username = '你的用户名'"
   ```

   ⚠️ 要删账号的话得**先删它的会话**：`sessions.user_id` 指着 `users.id`，
   而 D1 是开外键约束的——直接 `DELETE FROM users …` 会被挡下（变化 0 行，不报错，最容易被当成"删成功了"）：

   ```
   npx wrangler d1 execute fanyipigai-db --remote \
     --command "DELETE FROM sessions WHERE user_id IN (SELECT id FROM users WHERE username = '…')"
   ```

> 练习记录（每次批改、草稿、收藏）**仍然只在这台浏览器里**，没有跟着账号走：
> 账号这次只解决"谁在提交批改"与留言板/日志，云同步是另一件事（见「尚未开始」）。

### 改了代码怎么同步到线上

**自动（首选）**：`git push origin main` → Cloudflare 收到 GitHub 的推送就自己构建部署。

> ⚠️ **这条链子目前还差一步没接上**。线上那个项目是**用 Cloudflare API 建的**
> （绕过了控制台的授权向导），因此 GitHub 上那个 Cloudflare Pages App 很可能没被
> 授权访问这个仓库——实测两次 push 都没产生 `github:push` 类型的部署，部署列表里
> 只有 `ad_hoc`（我手动触发的那些）。
>
> 修法（一次性，两分钟）：GitHub → Settings → Applications → Cloudflare Pages →
> Configure → Repository access 里把 `fanyipigai` 加进去（或直接选 All repositories），
> 再回 Cloudflare 面板确认 Pages → fanyipigai → Settings → Builds & deployments 里
> Git 仓库指向 `2260863113/fanyipigai`。做完之后 push 就会自动部署。
>
> 修好之前，**手动那条路一直是通的**（下一条），所以不影响开发。

**手动（现在就能用）**：

```
npm run deploy
```

等于 `npm run build` + `wrangler pages deploy dist --project-name fanyipigai --branch main`。
它**把静态产物与 Functions 一起传**，因此改了 `functions/` 里的东西也一样生效
（这条实测过：能传上去，不会被"项目已连 Git"挡住）。不想敲命令也行——Cloudflare 面板
Pages → fanyipigai → 点 "Create new deployment"，那是同一件事。

**推 Gitee 不会触发 Cloudflare**：Cloudflare 只连着 GitHub，Gitee 那份是镜像，
推它是为了留一份国内可访问的副本。

**怎么确认线上是不是新的**：面板的部署列表每条都写着 commit 短号与提交信息，
对照 `git log --oneline -1` 即可。

### 本地把线上那一半也跑起来

不必部署就能在真 workerd 里验三个接口——`wrangler pages dev` 会在本机起一个真的：

```
npm run build
npx wrangler pages dev dist --port 8788
```

它读项目根目录的 `.dev.vars`（与本地开发同一份密钥），因此在 http://127.0.0.1:8788
打开的就是**线上那一套真实行为**：Pages Functions 的三个端点 + 静态产物，
一次批改真的会打到 DeepSeek。**这里已经没有登录环节**（见 ADR 0027），打开就是应用页面。

本地 `npm run dev`（5180 端口）跑的是 Vite 开发服务器那一套（含"改领域代码下次请求即生效"），
验收脚本（`verify-*.mjs`）对着它跑。

## 项目文档

- `CONTEXT.md` —— 领域词汇表（术语的准确定义，不含实现细节）
- `docs/adr/` —— 架构决策记录，说明"为什么这么做"以及"当初放弃了什么"
- `grill-rounds.log` —— 设计评审的轮次记录

## 明确不做

计时器与倒计时 ｜ 进步曲线与图表 ｜ 多用户注册与找回密码 ｜ 官方原文爬虫

## 目录结构

```
src/
  domain/          领域逻辑，不依赖界面
    types.ts       题目、作答、批改、错误对象、颜色分级等类型定义
    prompt.ts      AI 提示词（**三套**：批改的**分遍自查**（英译中五遍、中译英四遍，见 ADR 0021）、大改的整篇重写（不打分）、AI 出题的命题规格）
    refine.ts      大改档：逐句重写的类型、解析（原句按文字定位到译文、每句的原文定位到**原文**）与对照行（不打分，见 ADR 0015 与 0020）
    severity.ts    漏译/多译按字数定红橙（单位取**译文那一侧**，见 ADR 0014）
    submit-gate.ts 提交前的两道门：篇幅下限与"一字不差"去重（见 ADR 0018）
    parse.ts       解析 AI 返回的 JSON，结构不合格时给出可重试的具体原因
    validate.ts    批注位置校验：确认 AI 指的位置确实存在，对不上就拒绝渲染
    layout.ts      把批改结果转成可渲染的片段序列，并消解互相重叠的批注
    sections.ts    按自然段切分（splitSections）与**按页切分**（paginateArticle：一页 = 一个自然段，不足 50 单位与相邻段合并；顺带把每页的"原文/译文对"配好），以及结果合并
    generate.ts    AI 出题：篇长口径与硬校验、按题型截取（纯函数，不碰网络）
    custom.ts      自定义题：原文栏那个输入框里的字、方向/题型的自动判断、边写边存
    favorites.ts   收藏：改前/改后/原因 + 所在的整句，存进浏览器
    sentences.ts   断句的**唯一**判据：对照视图、句子题、收藏共用（差别只有"逗号算不算句末"）
    exercise-source.ts 一道题的原文在哪：五个来源统一查找，并给出"记录该显示的那一段"
    articles.ts    文章库的汇总与查询（正文在两个自动生成的数据文件里；五个板块、48 篇，见 ADR 0010 与 0011）
    articles-data/ 文章库正文与参考译文（**自动生成**，见 scripts/build-articles.mjs）
    terms.ts       术语库：两个术语范围下的官方译名、一并算对的写法、本地判分口径（第 13 轮换过整批，见 ADR 0023/0024）
    terms-data/    术语库正文（**自动生成**，见 scripts/build-terms.mjs）
    term-scopes.ts **术语范围**：与话题领域平行的另一张封闭表（国内/国际机关名称，见 ADR 0025）
    term-exercise.ts 术语题：题号 `term-v3-<范围>-<方向>`、按每页五条分页、逐条判分映射成 Correction
    legacy-terms.ts **归档的术语**：换代之前那 43 条政论术语 + 9 条古文名句，**不被任何代码引用**
    compare.ts     对照视图：切句 + 把改动拼成"修改后的完整那句"（纯函数）
    scoring.ts     系统计分：按错误分类与数量算分，不由 AI 打分
    color.ts       颜色分级
    ai.ts          DeepSeek 调用、按段并行、重试策略、失败分类
    failure-collector.ts 失败记录的**形状**与收集器（纯内存）：本地与线上共用它，因此这里不许碰 fs
    archive.ts     失败存档：把不合格的原始返回与上下文写入本地 .ai-failures/（只有本地能用）
    client.ts      前端调用 /api/judge 的封装
    mock.ts        11 道内置示例题与示例批改（覆盖四类题型，用于离线演示与测试）
  components/      界面
    App.tsx             编排层：状态、动作、把各栏拼起来（本身不再画栏与弹窗）
    session.ts          「每一道题各自的状态」的 reducer：作答/结果/看哪一面/第几份原文/生成的题池
    article-progress.ts 文章进度：哪几页批过（落盘）；据此续做、排序、标「已完成」
    last-view.ts        上次关掉网页时停在哪：栏 + 题 + 来源 + 页（落盘，下次打开落回那里）
    GradeHistoryPicker.tsx 「批改记录」下拉：这一页之前每一次批改，选哪次看哪次（见 ADR 0016）
    JudgeWaitingModal.tsx 提交之后的等待提示：两颗按钮「停留此页」「进入下一页」（见 ADR 0017）
    JudgeDoneToast.tsx  右下角的「第 x 页已经批改完成」，点它跳到那一页（见 ADR 0017）
    explain-lines.tsx   说明按分号断行 + 圈号（小卡片与大改的逐句解释共用同一份判据）
  RefineView.tsx      大改档的对照视图：一句原文、一句我的译文、一句修改译文、下面跟一段说明（第 4 条）
    CompareLines.tsx    对照行的渲染（精修档是大改档共用同一套排版；行首标签可换，大改档传「原文 / 我的译文 / 修改译文」）
    article-selection.ts 文章栏那两个下拉的选择（领域 + 方向，落盘）
    TopBar.tsx          顶栏：站名、题型导航、**暗夜开关 + 设置**（设置在最右；不再有 meta 元信息位）
    SourcePane.tsx      左上栏：原文（标题栏里是领域/方向 + 选择文章 / 换一换 / AI 出题；
                        术语栏换成「范围」+ 方向；自己贴的题则把正文换成**可写的原文输入框**）
                        + 正文末尾的「参考译文」折叠块 + 页脚固定的「上一页 / 下一页」
    DomainSelect.tsx    「领域」下拉 + 通用的「方向」两段切换（第 7 条之后长在原文标题栏里）
    TermScopeSelect.tsx 术语栏的「范围」按钮与**两屏弹窗**（两张范围卡片 → 中英对照平表 + 确定/返回）
    legacy-term-cleanup.ts 换代之前那些术语记录与收藏的**一次性清理**（判据是旧代次题号前缀）
    AnswerPane.tsx      右上栏：作答框或带批注的译文 + 视图切换 + **提交批改 / 返回编辑（同一颗按钮）**
    AnswerViewSwitch.tsx 「批改视图 / 对照视图」分段按钮（练习页与练习记录页共用）
    ScorePane.tsx       左下栏：分数与错误归类
    NotesPane.tsx       右下栏：当前点中的那一处的说明 + 「查看 AI 完整返回内容」
    Modal.tsx           弹窗外壳（共用的那几处，避免逐字重抄）
    SettingsModal.tsx   设置：行距、译文视图、是否显示填补文字
    GenerateModal.tsx   AI 出题：方向 + 文体 + 领域
    AnnotationText.tsx  批注渲染、调序弧线、点中后浮出的气泡
    annotation-summary.ts 每一处批注「哪段文字 → 改成什么 → 为什么」的唯一来源
    DetailPanel.tsx     一处批注的完整说明，底部是「收藏」
    RawResponseButton.tsx 「查看 AI 返回完整内容」+ 屏幕中央的弹窗
    CompareView.tsx     对照视图（一句一句，改前 / 改后）
    TermRows.tsx        术语题的作答栏：在写是五个等分输入框，判完是五条带颜色的批注行
    settings.ts         界面偏好：行距、是否显示填补文字、默认视图、**明暗主题**（存 localStorage）
    page-state.ts       **每一页的界面状态**：草稿、是否在编辑、正在看第几次（第 10 条，落盘）
    split-layout.ts     **分割线位置**的三组值（第 5 条，落盘；双击恢复默认也写回）
    AnnotationList.tsx  全量清单（只用在练习记录页）
    ScoreSummary.tsx    分数与错误归类的具体呈现（由 ScorePane 包一栏）
    RecordsView.tsx     练习记录页（导航栏的一栏，两屏之间也能拖；详情栏有「删除这一条」）
    FavoritesView.tsx   收藏页（导航栏的一栏）
    FixLayer.tsx        上方补写的字：先量再摆，跨行时一段对一行
    split-drag.ts       分隔条的拖动 + **位置落盘**（练习页与记录页同一套）
  domain/submit-gate.ts  **提交前那两道门**：篇幅下限与「一字不差」去重（判据只写在这一处）
  server/          服务端：本地与线上**共用**的那一半（见 ADR 0026）
    api.ts         三个接口的共用实现：校验、编排、日志文案、什么时候归档
    host.ts        线上宿主：把 Cloudflare 的环境变量与请求翻成 ApiHost
    db.ts          D1 的最小结构类型（不引 workers-types，理由见 ADR 0026）
    http.ts        账号那一套的底座：Env/Ctx、JSON 响应、ApiError、handle()
    auth.ts        会话 token 的签发与校验（库里只存 SHA-256 摘要）
    guard.ts       requireSession / requireAdmin：**服务端才是登录边界**
    rows.ts        D1 行 → 对外 JSON（服务端绝不返回密码哈希）
    validate.ts    用户名归一化、密码哈希结构校验（迭代次数下限）
    limits.ts      留言长度、头像体积、用户名长度等共用常量
    profile.ts     头像与改密码的校验（改密码要先验旧哈希）
    board.ts       留言内容清洗与分页参数
    statsWindow.ts 访问日志的分桶（逐字搬自来源项目，它自带单测）
  components/auth/  账号前端：api.ts（接口封装 + 浏览器端 PBKDF2）、store.ts（会话 store）、
                    format.ts（时间/UA）、AuthModal.tsx（登录注册弹窗）
  components/ProfileView.tsx 个人中心（头像、用户名、改密码、退出）
  components/BoardView.tsx   留言板（谁都能看，登录才能发）
  components/AdminView.tsx   管理页（用户管理 + 日志记录 + 流量分桶，仅管理员）
functions/         Cloudflare Pages Functions（线上那一半的真路由；共享代码不许放这里）
  _middleware.ts   只补三个安全响应头（nosniff / 不许被 iframe 套住 / 不带 referrer）
  api/judge.ts api/refine.ts api/generate.ts  三个薄端点，转手交给 src/server/api.ts
  api/auth/{salt,register,login,logout,me,profile}.ts   注册登录与会话
  api/board/{index,[id],reply/index,reply/[id]}.ts      留言板（只能删自己的）
  api/admin/{users,logs}.ts                             用户管理 + 访问日志（仅管理员）
  api/announcements.ts                                  公告列表（公开可读）
  api/admin/announcements{,/[id]}.ts                    发布/修改/删除公告（仅管理员）
  api/visit.ts                                          访问上报（不含 IP）
schema.sql         账号/会话/留言板/访问日志/公告五张表（D1）
wrangler.toml      Pages 项目名、构建输出目录与 **D1 绑定**
vite-plugin-judge-api.ts  本地开发用的 /api/*（只剩宿主那一层：密钥、日志、存档；逻辑在 src/server/api.ts）
启动.bat / start.ps1      Windows 一键启动
start.sh                  macOS / Linux 一键启动
scripts/
  smoke.ts               冒烟测试的检查项
  render-probe.tsx       在 jsdom 中挂载界面，填写作答并模拟提交（含接口模拟响应）
  visual.ts              真实浏览器截屏（CDP 驱动，注入接口桩、填作答、点提交）
  probe-fix-align.mjs    真实浏览器里量「补写的字」与下方荧光笔带子的对齐（含拖动分隔条之后、跨行拆段、插入空位）
  measure-cost.mjs       量一次批改花多少 token（官方 tokenizer 数提示词 + 真实调用的 usage）
  shots.mjs              单独生成截屏
  fixture-stub.mjs       注入页面里的批改接口桩
  fixture-payload.mjs    算出桩要返回的批改结果（与真实接口同形）
  run-smoke.mjs          用 esbuild 打包后交给 Node 运行
  live-judge.mjs         用真实 API 跑一次句子题批改
  live-refine.mjs        用真实 API 跑一次大改档（整篇逐句重写 + 每句原文 + 逐句解释，不打分）
  live-sections.mjs      用真实 API 验证文章题的按段并行与序号换算
  build-articles.mjs     从 文章.docx 生成文章库正文与参考译文（唯一生成入口）
  check-articles.mjs     文章库体检（篇数 / 完整性 / 不雷同 / 译文对齐 / 分页）
  build-terms.mjs        从两份机关名称 docx 生成术语库正文（唯一生成入口，第 13 轮新增）
  check-terms.mjs        术语库体检（条数 / 分页 / 别名与拼写变体 / 不许放宽过头，第 13 轮新增）
  verify-article-bar.mjs 真实浏览器验收文章栏（领域/方向在原文标题栏、选文章、参考译文折叠块、暗夜模式、续做与记忆）
  verify-per-page.mjs    真实浏览器验收逐页批改（每页各交一次、不串页、翻回不重提、提交门、删记录、分割线记忆）
  verify-term-mode.mjs   真实浏览器验收术语栏（范围弹窗两屏与对照表、17 页翻页、末页三条可填、本地判分、返回编辑后五条各回各框）
  verify-sentence-mode.mjs 真实浏览器验收句子题
  verify-accounts.mjs    接口级验收账号体系（注册/登录/会话/资料/留言板/权限/日志；需要本地 D1 + wrangler pages dev）
  verify-login-gate.mjs  真实浏览器验收登录门（没登录按提交 → 一次接口都不发、弹出登录窗；注册后顶栏出现用户名）
  lib/probe-answer.mjs   造"过得去提交门"的假作答（探针 / 截屏 / 验收脚本共用同一份口径）
  failures.mjs           查看失败存档
```

## 实现进度

**已完成**

界面与批注

- 顶部导航栏：`文章` `段落` `句子` `术语` 四个题型 + **`自定义`**（贴自己的题）+ **`收藏`** + 独立的 **`练习记录`** 一栏
- 左右两屏布局：左屏整页原文（正文结束后有「参考译文」四个字，点一下铺出这一页译文），右屏整页作答
- **批改结果的右屏下方分成左右两栏**：左栏计分与错误归类，右栏**批注详情（只显示点中的那一处）**
- **文章模式逐页批改**：一次只显示一页，页下有「上一页 / 下一页」。翻页**只是翻页**（不提交、不拦人，
  草稿按页留着），提交只有一个触发点——用户按「提交批改」；交出去之后这一页只读并显示进度条，
  但**照样能翻到下一页接着译**，批完在右下角弹一条通知、点它跳回那一页。批过的页只读。
- **这一批（第 2／3／5／7／8／9／10／11／12 条，见 ADR 0018 与 0019）**：
  提交门（篇幅 + 去重，吐司说原因）、提交批改与返回编辑是同一颗按钮、
  原文栏改造（领域/方向进标题栏、正文末尾的「参考译文」折叠块、翻页固定在栏底、
  删掉「对照译文」与两枚信息芯片）、整站暗夜模式（跟随系统 + 手动记忆）、
  页状态落盘（草稿/编辑态/正在看第几次）、分割线位置落盘、批改记录可删（练习记录同步）、
  文章卡片上的「最高分 N 分」
- **再一批（第 1／4／6／13／14 条，见 ADR 0020／0021／0022）**：
  **大改档取消分数与总评**（全站只剩精修档一种分数）、大改对照改成**四块一组**
  （一句原文、一句我的译文、一句修改译文、一段说明）且**右栏整栏、右下角那一栏不画**、
  大改提示词要求**每句给出对应的原句**（程序拿它去原文里定位，对不上就标一下）；
  **英译中·精修改五遍**（漏译 → 术语 → 啰嗦 → 更好的表达/用词 → 语法与标点）并新增
  「啰嗦」分类；**漏译按字数扣分**并在分数栏单独列出来；**雷同片段不标色**
  （改动块两端相同的整词/整字也不划，中文整句标色的老毛病一并修掉）
- **第 13 轮（术语库整批换代 + 三处界面返工，见 ADR 0023／0024／0025）**：
  **术语栏整批换成两份机关名称 docx**（国内机关名称 83 条/17 页、国际机关名称 60 条/12 页，
  由新脚本 `node scripts/build-terms.mjs --write` 现算；旧 43 条政论术语归档进 `legacy-terms.ts`，
  不被任何代码引用），**术语范围成了与话题领域平行的一张自己的表**（「范围」按钮 + 两屏弹窗 +
  中英对照平表，「确定」落到第 1 页），**术语栏照搬文章模式的逐页批改**（一页五条、末页空行不可填、
  翻页不提交、批过的页只读、同一颗按钮「提交批改／返回编辑」、进度也管"没批完的那一页"），
  **判分口径放宽**（官方缩写／英美拼写照词表／重音符号／开头的 The 都算对，一英多中两个都算对，
  屏幕上用「／」列出来），**顶栏那枚「内置示例批改」芯片整枚删掉**
  （术语本地判分不再借用 `fixture` 身份，改用新来源 `local`；示例提醒搬进结果栏），
  **自定义栏不再弹窗**（原文栏自己就是输入框，边写边存、题号只铸一次；
  "练过之后再改原文"当成新题换题号，免得旧记录跟着改口），
  术语栏撤掉「换一换」与「AI 出题」。
  > ⚠️ 大改界面（左栏上下那套版式）用户最后选了"忽略这个要求"，因此**一个字没动**。
- **服务端（线上那一半，见 ADR 0026）**：三个接口从"只有本地开发插件里才有"补成
  **一份实现、两个宿主**（`src/server/api.ts` 共用；本地由 Vite 插件调、线上由
  `functions/api/*.ts` 调）；失败存档线上进服务端日志，记录形状与本地一字不差。
  **访问口令门当天即按用户要求拆掉**（"去掉进入口令这一环节，任何人都可以访问"，见 ADR 0027）：
  门、登录页、签名 cookie 与那两个 Secret 一起删除，只留下三个安全响应头；
  想收回来 `git revert 50b7622` 即可
- 五种改法全部实现：替换、插入、删除、整句重写、语序调换（含配对弧线）
- **正确写法写在被改内容的上方，互不重叠**（`domain/fix-layout.ts` + `components/FixLayer.tsx`）：
  默认居中于被修改内容、挤了左右分开、分不开就往上加一层；补写的字比被改内容宽时两边加空档撑到等宽；
  跨行时**一段对一行**。**宽度不受被改内容限制**（内容多长就多宽，超宽靠加层解决），
  唯一的边界是译文栏本身
- 这些字**只有文字、没有框**：不加边框、底色、阴影、内边距（用 `span` 而不是 `button`——
  `button` 自带浏览器默认的灰底与边框，CSS 里不写 border/background 也挡不住它）。
  字号取小一号（0.78em），行高 1.6，**也不加粗**（所有"修改后的内容"一律与正文同粗）
- **居中这件事是每次量完再算的，不是算一次记住**：位置以"撑开之后的锚点矩形"为基准，
  而且受栏宽变化驱动重算（`ResizeObserver` 盯着译文栏的宽与整段文字的高）——
  拖分隔条改的是**栏宽**，窗口尺寸一点没变，`window.resize` 根本不会来。
  早期版本量完还留着一份旧坐标，拖过分隔条后会整整偏出 94px
- **荧光带跟着补写内容加宽，并且左右各再多留 4 个空格**（最终口径）：
  - `带子宽度 = 补写内容一行排完的宽度 + 左右各 4 个空格`（按补写字号量，实测 13.13px/侧）
  - 补写内容**永不折行**（`white-space: nowrap`）：一行放不下就用空白继续把带子拓宽，
    这正是用户的原话"如果需要换行，就继续用空白拓宽颜色标记范围"
  - 撑开只按**没撑开时**的天然宽度算一次，不来回调：空档一变大，被改内容自己就会重排、行宽全变，
    再照新行宽去调就成了自激（实测把一处撑到 174px、整块挤成一行）。
    撑开之后的少量出入由**切断点**吸收——切断点放在撑开之后、按**最终行宽**分
  - 一条边界：带子不许横着跑出译文栏，右边那份空档被栏宽顶住时，多余的挪到左边
    （左边的空档只是把被改内容自己往后推，推过头它会换行，不会溢出栏）
  - 实测（`--synthetic`，栏宽 768px）：补写的字 311.4px → 带子 337.6px（+26.25px，正是 8 个空格），
    方框 311.4px × **21.84px（一行）**，离带子边缘左 13.13px / 右 13.12px、越出带子 0.00px；
    `i is → I am` 同样一行：方框 27.5px、带子 53.8px、两侧各 13.1px
- **补写的字跟随原译文换行：一段对一行**。
  被改内容跨两行时，把补写的字按**各行最终行宽的占比**在词与词之间切开（词内部绝不切开），
  一段对一行、每段各自居中于那一行的带子；每段仍然只有一行。
  实测（栏宽 549px）：带子两行 145.8px + 191.8px（合计仍是 337.6px = 补写内容 + 8 个空格），
  两段方框 142.6px 与 165.5px，都在各自带子里面、都没有折行
- **被标记的原文允许在词与词之间断开换行**（放不下时才断）。
  曾经 `.mk-replace` 被锁了 `white-space: nowrap`，行尾放不下时那一整块会被顶到下一行去——就是"捆绑着一起换行"
- **荧光区域里只有被改内容**：那两段空档是真空档，**相邻的文字被它推到带子的左右两侧**去了，
  不会压在荧光上（这正是"真空档"与"用负 margin 抵消掉的空档"的区别）。
  实测（探针里按像素数，`--ink-negative` 是它的对照）：
  空档那两段里 **0 个字**的像素、被改内容自己那两行有 360 个字；把旧的画法（负 margin）
  放回来，同一位置立刻数出 **201 个字**压在带子里
- **插入点画成一块荧光笔空位**（不再用那根零宽的小竖线）：`FixLayer` 量出补写内容的宽度后
  写在这块空位上，于是上方那个方框与它**一样宽、左右都对齐**。
  实测（`--synthetic --insert`）：空位 20.13px、方框 20.14px，左偏差 0.01px、右偏差 0.00px。
  没开「显示填补的正确写法」时量不到宽度，CSS 里那个 0.6em 是兜底，免得插入点整个看不见
- **上下间隔很近**（`fix-layout.ts` 的 `FIX_BOX_TOP_GAP`）：方框底边贴着锚点行内容区的顶边。
  这个 0 不是"贴在一起"——方框那一行自带半个行距的空白，锚点行的内容区顶边也在字身最高点上方，
  两边的空白加起来仍有 7–10px 的墨迹间隔（早期取 6，补写的字明显飘在半空）
- 它们需要更多竖直空间时**自动加大正文的行间距**（只补原来行距放不下的那部分），
  因此"补写的字压到上一行文字上"不会发生
- **补写的字与圈号都不占行内宽度**，所以批注不会挤动译文；文字流与作答**逐字相同**（冒烟测试守着这条）。
  撑着的那两段空档是真占地方的（相邻文字会被推开），但它推的是**段落自己的排版**，
  一个字都没改、也没少——见上文「荧光区域里只有被改内容」
- **最小不同项按"词"算，不拆到字母**：漏了一个词就只补不勾（`<the>`），多了一个词就直接勾出；
  词形变化则勾出整个词、上方写新词（详见上文「最小修改」）
- 颜色由分类推导：红＝硬性错误，橙＝表达问题，绿＝表达优秀（三色共用一套荧光笔底色）
- 修改风格切换：精修（逐处批注 + 程序算分）／大改（整篇逐句重写 + 每句原文 + 逐句解释，**不打分**、只给对照）
- **系统计分**（不由 AI 打分）：满分 100，硬性错误每处扣 8，表达问题每处扣 3
- 批改结果只保留计分与错误归类，**没有 AI 写的总体评语与分项评语**
- **点哪一处看哪一处**：点译文上的勾画 → 那一行旁边浮出**小卡片**（**完整**说明）+ 右下角换成这一处的**大卡片**；再点同一处、点别处、或按 Esc 都会收起来。
  **点上方补写的字同样算选中这一处**（判定范围只是扩大了，语义没变）——
  它以前被文档级那句"点外面就收起来"当成外面，点上去等于没反应。
  卡片里**不写**「某某 → 某某」：改前改后本来就画在译文上（荧光带 + 上方小字），再抄一遍只是占地方；
  卡片只说"这是什么问题、为什么"，完整说明在右下角。卡片里的「说明」遇到**分号**会断行，
  **每一行开头带一个圈号（①②③…）**，右上角那个序号写作**彩色的（1）（2）**（用这一处自己的颜色）
- **两张卡片里点哪儿都不会把卡片关掉**（用户要求）：判定看的是"点在不在卡片里"（两张卡各带 `data-card`），
  不是一张类名清单——从前点到**大卡片的正文**会被当成"点了外面"，卡片当场消失。
  小卡片另有一件事要配套：它整张**收**鼠标事件（从前是 `pointer-events: none`，
  那一击会穿过卡片落到下面的元素上，判定看来同样是"点了外面"）。
  代价是卡片正下方那几个字上的勾画点不到了——卡片不高，用户明确选了这个取舍。
  > ⚠️ 这两件事浏览器探针都抓不到"没反应"：jsdom 没有命中测试，`.click()` 又绕过它。
  > 因此 `verify-per-page.mjs` 现在用 `elementFromPoint` 拿到的**真实元素**去点卡片正文，
  > 并量"卡片中心命中的是不是卡片本身"；`smoke` 里另有一条样式表断言守着 `pointer-events`。
- **小卡片是一块浮层，不被任何栏挡住**（用户要求）：`position: fixed` + 挂在 `document.body` 上，
  因此不会被译文栏的滚动切口剪掉，也不会被下面那两栏压住；下方放不下时翻到那一行上面。
  真实浏览器验收里量到过它的中心点落在视口之外——那正是"算错位置就等于卡片没出来"。
- **收藏**：气泡打开后，气泡脚下与右下角那一栏底部**各有一颗「收藏」**（同一个开关）——
  点它就把这一处的**修改前整句**与**修改后整句**，连同"为什么"、以及当时是哪道题一起存下来
  （再点一次就是取消）。收藏那一栏里两句都摊开，**只标这一处**（同一句里别的错误一律不标）：
  **改前那句**上的是荧光底色 + 同色文字（荧光只出现在原译文里），**改后那句**只标字体颜色、不加底色。
  可以逐条删或全部清空。
  存整句而不是只存两个词，是因为单独的 `farmer → farmers` 过几天就不知道在说什么了；
  也连"哪道题、什么方向、什么题型"一起存，因为练习记录会变、题库会更新，只存一个编号迟早指不到
  > 卡片那一颗**必须收得到点击**：卡片从前整张是 `pointer-events: none`（免得挡住底下的勾画），
  > 那条规则会一起作用到里面的按钮上，于是"点收藏没反应"（用户报过两次）。
  > ⚠️ 这个坑浏览器探针抓不到——jsdom 没有命中测试，`.click()` 又绕过命中测试，
  > 所以旧脚本里它一直是绿的。现在由 `verify-per-page.mjs` 看**计算出的 `pointer-events`**
  > 加一次 `elementFromPoint` 命中测试来守，`smoke` 里也有一条样式表断言。
- 一处错误横跨多个词时各画各的，但共用同一个编号（点哪一边都是同一处错误）
- 右下角标题栏右侧可以「点击查看 AI 返回完整内容」：屏幕中央弹出模型返回的全文
- **大改档走另一条渲染路径**（见 ADR 0015）：译文栏只给「原译 / 改后 / 为什么这么改」三行的对照，
  视图开关**保留但禁用**并注明原因；左下角**不打分**（只写一句"大改档不打分"，见 ADR 0020）；
  右下角那一栏**整块不画**，那一块地方合并给右边整栏的对照（第 4 条）。
  每一组排成**四块**：一句原文、一句我的译文、一句修改译文，下面跟一段说明（说明按分号分点换行）。
  大改档**没有勾画、没有批注清单、没有错误归类**（这是"不统计、不逐处批改"的意思），
  因此收藏按钮也不出现；练习记录里回看那一次时同样是这一套
- **AI 出题**：选方向/文体/领域现出一篇完整文章（配参考译文），结果按题目留存、可用「换一换」翻回来；
  非文章题型从全文里截取对应大小；篇幅由程序硬校验，不达标带着原因退回重写
- 段落模式也有「换一换」了（`variants.ts` 里补了段落·中译英的备选）
- **「自定义」：原文栏自己就是一个输入框**（`domain/custom.ts`）。导航栏多一栏「自定义」，
  **进去不弹任何窗**（第 13 轮用户要求："一进去不弹出窗口要求输入，而是将原文也变成输入栏，
  用户自行粘贴原文，自己输入译文"）：原文栏就是那个可写的输入框，边写边看。三条约定：
  - **只写原文**：方向按"有没有汉字"自动判断（有汉字＝中译英），文体按一般说明文算，
    参考译文留空——批改本来就不需要它（参考译文从不外发，也不会因此多花钱），
    那一块干脆不摆空壳子
  - **题型程序自动判断**：多个自然段→文章题（逐段作答）、两句以上→段落题、
    很短又没有标点→术语题（中文按 ≤8 字、英文按 ≤4 词，中文术语常见 2–8 字，口径不能用同一个数），
    其余→句子题；标题栏里如实标出"自动判定 · 中译英 · 句子翻译"，篇长硬校验（英译汉 250–350 词那套）**不适用**
  - **只留最新一篇，存在这台浏览器上**（与设置、练习记录同一个存法）；题号**只铸一次**，
    边写边存时一个字都不换——每敲一个字换一个题号会把作答、批改结果与页状态全丢掉。
    ⚠️ **已经练过这一篇之后再改原文，就当成一篇新题、换一个新题号**：
    记录里**不存题干**（靠题号回查），不换的话旧记录会跟着新原文改口
  - 自己贴的题不给「换一换」「AI 出题」「重新贴一篇」（**"贴一篇"那颗按钮随弹窗一起删掉了**：
    原文本身就是输入框，想换一篇直接改上面的字）
- 练习记录独立成页：左边列出全部记录，右边显示当时的完整批改——**译文上同样带着勾画**，
  回看时不必自己重新定位（清单仍是全量，因为记录里还要能逐条翻）。
  ⚠️ 逐页批改之后**一条记录 = 一页**（带 `sectionIndex`），列表上写的是"第 N 次 · 第 K 页"；
  **「原文」那一栏显示的是这一页，不是整篇**（标题里写着"原文 · 第 N 页"）——
  用户要的就是"收藏模式下，原文应该是当前一段的原文"。句子/术语这类不分页的题，
  显示的就是它们自己的那一段（标题里只写「原文」）。五个来源的查找集中在
  `domain/exercise-source.ts`，分页用的是与练习页同一份规则，因此拿到的永远是"当时屏幕上那一段"。
  存储键也因此升到 `records.v2`——v1 里一条是整篇、v2 里是一页，混着读会让界面理直气壮地显示对不上的东西

AI 层

- 批改由真实 DeepSeek API 完成，密钥只在服务端使用
- **按页并行批改**：长文本按段落拆分，每段一个请求并行发出（并发上限 4），
  总耗时取决于最慢的那一段。实测英译中 4 段文章：**并行 38.5 秒**（串行约需 2 分钟）。
  逐页批改之后每次只提交**一页**，所以单次更快，但一整篇会分成几次调用（见下面的成本表）
- 段落起点换算：每段返回的段内序号会被换算到全文坐标，合并后再校验一次
- 提示词允许**合理意译**，但术语与固定表述必须一字不差
- AI 返回结果的解析与位置校验：不合格时把具体原因回传重试，最多 3 次
- 位置校验：AI 指的位置与译文对不上时拒绝渲染，并指出偏了几个字符
- 重叠批注消解：长跨度批注优先，避免划线上再划线
- 失败分类提示：密钥无效、余额不足、限流、超时、返回被截断，各自给出不同的下一步动作
- **失败存档**：失败的原始返回与完整上下文写入 `.ai-failures/`，供迭代提示词

题库

- 11 道内置示例题，覆盖四类题型与两个翻译方向
- 文章题按官方规格写作（英译汉约 260 词、汉译英约 280 字），便于核对篇长校验的目标

**验证方式**

- `npm run smoke` 共 828 项检查，全部通过（含 26 条最小修改用例（**雷同片段不标色**＝改动块两端相同的整词/整字也不划，第 6 条，见 ADR 0022）、**提交前那两道门**（多于 30 个单位、只管文章题与段落题、去重按去掉首尾空白后逐字符比、删掉的记录不再拦人，并且**拿探针那份假作答复核一遍门槛**，防两边口径漂掉）、**每一页的界面状态**（草稿/编辑态/正在看第几次按「题 + 页」落盘、内容没变一个字都不写、换原文整道题清掉、200 页上限按最久没动过的先丢）、**分割线位置**（三组各记各的、双击恢复默认也写回、越界比例夹回来、存储坏了当没记过）、**明暗主题**（默认跟随系统、手动选过之后不再听系统的）、**删掉一条记录**（练习记录与下拉同步少一条、其余留着）、**最小匹配四组形状**（共同前缀／共同后缀／中间相同／只多一个词都不标色）与那条真实案例、**漏译/多译按字数定轻重**（单位取译文那一侧、正好 3 个单位就算红、颜色与扣分同源）、**大改档**（解析三件套缺一不可／原句由程序定位／一字不差的不算改过但仍然出现在对照里／颜色一律橙不出现红／**真界面**里提交打到 /api/refine、只给对照、开关禁用、分数标"AI 总评"）、**文章分页**（一页 = 一个自然段／不足 50 单位的并成一页／**够长的段独占一页**／超长段不切／页与页只隔空白且拼起来一个字不丢／每页的译文逐段对上）、**文章进度**（批过哪几页→从没批完那页续做／练完的排到最后）、**「批改记录」下拉**（放开之后看得到那一次、点它切回当时那份批改且不发请求、改过字之后它仍然在）、**切栏再切回来"正在看第几次"还在**（用户报的那条）、**点两张卡片的正文都不会关、小卡片挂在 body 上是固定定位、大改的解释按分号断成①②、没改字就翻走再回来仍是批改界面**（本轮的六条）、**改过之后手动提交要重新只读并显示新结果**（回归）、**记录与收藏里的原文是"那一页"而不是整篇**、**记录的参考译文是"那一段"的**、填补文字的不重叠摆放／透明无框／跨行拆段／永不折行／插入空位／带子按"补写内容 + 左右各 4 个空格"撑开、记号以底色为主（**纯删除额外加一道横线**）、**收藏按逗号也断句**（中英文逗号都验）、**对照视图与收藏的分句逐字一致**（引号收尾的三种写法都要切成三句，而逗号只在收藏里断）、**句号漏空格的那一段在对照视图里出三行**（用户第三次报的那条／`U.S.A` 与 `3.5` 照旧不切／`82,000` 不被逗号截断）、卡片不抄改前改后／序号是彩色的（N）／说明按分号断行且每行带圈号、**提示词要求每条说明的每一小点用「；」隔开**、收藏存下改前改后整句与两段上色范围、**气泡脚下那一行必须把 pointer-events 收回来**（否则卡片里的「收藏」点不动，而这一点浏览器探针查不出来）、**术语输入框无边框**、**术语判分映射出的区间是真的**（说明／收藏／对照视图都靠它）、**术语库换代后的数据与口径**（83/60 条与 17/12 页、末页余数、官方缩写与英美拼写（含 `Programme`/`Program` 那个子串陷阱）／重音符号／开头的 `The` 都算对、一英多中两个都算且屏幕上用「／」连起来、题号 `term-v3-…` 能来回解析而 `term-v2-…` 一律失败、题干按页切分逐页对上、五条答案"拆得回各自那个框"）、**对照视图"原译 / 改后"交替两行**、自定义题的方向与题型判断／存进浏览器／贴题流程、练习记录页的分隔条可拖、**逐页批改**（会话 reducer 按页存结果／翻页不动别人的结果／「返回编辑」不丢结果、改一个字才作废／「重新作答」另走一个动作真丢结果／多页流程**翻页不提交且草稿留着**、提交后弹等待窗、批改中翻页、一次只批一页、批完的通知可跳转、三页各留一条落盘记录、每页请求发的是自己的文字、**发给模型的原文只有当前这一页**）、**术语栏五等分与分割线**、设置、参考译文不外发、AI 出题与命题依据、**"AI 出题的领域预设与文章库的板块表逐个对齐"**、界面交互、练习记录与截屏）
- **逐页批改在真实浏览器里也验过**：`node scripts/verify-per-page.mjs [地址]`（对着**已在运行**的服务跑，
  批改接口用桩并记下每一次请求）：**点「下一页」不触发提交批改**（而且翻回来草稿还在）、
  提交后按钮变"批改中…"且作答框只读、等待窗两颗按钮（末页不弹）、
  **批改还没回来就能翻到下一页**、此时在别的页上写了字也提交不了（一次只批一页）、
  批完右下角弹「第 x 页已经批改完成」且**点它跳回那一页**、每一页各发一次请求、**不串页**
  （每次发的都是那一页自己的文字）、翻回已批过的页**不重新提交**、批过的页只读、
  **每一次请求的答案起点都是 0**、小卡片那颗「收藏」**真的收得到点击**（命中测试）、
  收藏页每条都贴出"这一处所在的那一段原文"，另外还有第 2／3／5／10／11／12 条那一批：
  **提交门四步**（太短拦下且一个请求都不发 / 够长放行 / 一字不差拦下 / 改一句又放行）、
  **删记录**（叉号 → 确认 → 练习记录与下拉同步少一条）、**分割线位置记住**、**记住正在看第几次**、
  **最高分标签批过之后是真实分数**、**刷新后编辑态与草稿仍在**；
  第 4 条那一批也在这里量**真实矩形**：**大改档的版式**（左栏上原文、下总评，右栏整栏都是对照，
  右下角那一栏 `display: none`）、三行标签是「原文 / 我的译文 / 修改译文」、每组四块、
  **左下角只写"大改档不打分"且一个数字都没有**、（记录下拉里也标「不打分」） —— 124 项断言全过。
  最后几项用的是**服务端那道请求判据本身**（从 vite 插件的 `isCorrectionRequest` 打包出来再 import）：
  曾经因为"发整篇原文分段配一页作答"而每次提交都被 400 挡回来，而桩只记录请求、不会报这个错。
  为了让"批改中"那段时间真的存在，桩会把它拖慢（第一次 5 秒给进度条采样、之后每次 1.5 秒）。
- **术语库有独立体检**：`node scripts/check-terms.mjs` 验两个范围的条数与页数、每页 1–5 条与末页余数、
  题号换代（`term-v3-…` 解析得出、`term-v2-…` 一律失败）、**每一条的答案都不能被别的术语接受**
  （唯一例外是登记在 `zhAlt` 里的一英多中——这条盯的是"放宽过头"那类不报错的 bug）、
  别名与英美拼写变体确实算对、题干按页切分逐页对上（**170 项**）
- **文章库有独立体检**：`node scripts/check-articles.mjs` 逐篇验"每格篇数、都是完整的一篇、
  同一格不雷同、正文与译文段数相等、**分页结果与规则的手写实现逐页一致**、拼起来一字不丢"，
  并打印页数分布（当前 **48 篇 / 201 页，其中 180 页就是一个自然段（90%）**；
  篇长分布：3 页 9 篇／4 页 23 篇／5 页 14 篇／6 页 2 篇）。
  它在实现之外**另写了一遍分页规则**再逐页对照——只写"不许有小页"这类不变量是能蒙过去的
  （把整篇并成一页也不违反它）
- **文章栏在真实浏览器里也验过**（`node scripts/verify-article-bar.mjs`，75 项）：
  **领域与方向长在原文标题栏里**（领域紧挨「原文」三个字；顶部那条横条已删）、
  卡片是"事件锚点当标题 + 篇幅 + 共几页 + **最高分 N 分**"且**没有来源媒体**、
  **正文末尾的「参考译文」折叠块**（点开→铺出这一页译文→再点折回；没有译文的题连这四个字都不出现）、
  **顶栏不再挂方向/文体/话题三枚标签**、**暗夜模式**（顺序「暗夜 · 设置」、切换生效、存下来、刷新不回弹）、
  续做与记忆两条路（没记住页号时从没批完的那一段继续 / 记住了页号时回到那一页、那一栏）、
  练完的不再主动打开、列表里排到最后并标「已完成」
- **另外三个浏览器验收也跟着跑了**：`verify-per-page.mjs` **124 项**（翻页不提交、批改中可翻页、
  一次只批一页、批完的通知可跳转、**点卡片正文两张卡片都不关**、**小卡片中心命中的就是卡片本身**、
  档位名是「精修 / 大改」、逐页提交、不串页、翻回不重提、提交门、删记录、分割线记忆、
  **大改档的版式与"不打分"**）、
  `verify-term-mode.mjs` **109 项**（见下）、`verify-sentence-mode.mjs` 15 项 —— 全过
- **术语栏在真实浏览器里也验过**（`verify-term-mode.mjs`，第 13 轮重写）：
  **范围弹窗两屏**（两张卡片上的"83 条 · 共 17 页"、点开是一张 83 行的中英对照平表、
  「返回」退回两张卡片、「确定」切过去并**落到第 1 页**）、**17 页翻页**（页脚那句
  "第 N / 17 页 · 待批改"、第 1 页「上一页」禁用、末页「下一页」禁用、翻走再回来草稿还在、
  翻页一次请求都不发）、**末页只有三条**（画五行、三条可填、另两行禁用且不可填，
  但写上那三条就能提交）、**本地判分**（对错记号的形状、译错的划掉并在同一行给出官方译名、
  判完这一页只读、**请求数为 0**）、**「返回编辑」那颗按钮**（点它回到五个输入框，
  而且**五条答案各自回到自己的框**——用户报的就是"全塞进第一个框"）、
  方向切换（英译中把题干换成官方英文、一英多中在屏幕上用「／」列出来）、
  以及**顶栏没有那枚「内置示例批改」芯片**
- **三个真实 API 用例实测通过**，批注位置、错误分类、术语判定均正确；其中一次 AI 返回
  坏 JSON 自动重试后成功，一次 AI 数错序号被位置校验拦下
- **大改档也真调过一次**（`node scripts/live-refine.mjs`，两个用例各一次真请求）：
  英译中那句给 82 分、逐句重写并把"候鸟喜欢的到达地"改成"候鸟青睐的热门栖息地"；
  中译英那两句给 55 分、把"绿水青山就是金山银山"改回官方译法 `lucid waters and lush mountains
  are invaluable assets`、并逐条说清主谓一致、冠词、缺少 be 动词这几处硬伤。
  两例都按格式返回（一例首次返回没通过校验、自动重试后成功），
  证明确实是"模型真的会按这个格式返回"，而不只是"程序接得住一份形状正确的返回"
- 通过本地接口的端到端实测：10.8 秒完成
- 界面截屏由真实浏览器生成，供人工确认（见上文「界面截屏」）
- **对齐是量出来的**：`node scripts/probe-fix-align.mjs` 在真实浏览器里逐段量
  「补写的字」与「它正下方那行荧光笔带子」的左右偏差、宽度差与上下余量，并且**拖一次分隔条再量一遍**；
  顺带体检记号：点勾画要能出气泡。
  句子题实测：`i is → I am` 左右偏差 0.00px、宽度差 0.00px（空档 5.47px 全在右边——它在行首，
  左边没有地方可撑）；跨行那道构造题：带子两行合计 337.6px（＝补写内容 311.4px + 8 个空格），
  方框都在带子里面，居中误差 0.01px，越出带子 0.00px（见上文「荧光带跟着补写内容加宽」）
- **"荧光里有没有别人的字"是按像素数的**：`measureInk()` 把译文栏那一块截下来
  （`probe-fix-align.mjs` 里自带一个几十行的 PNG 解码器，只用得上 8 位 RGB/A），
  数空档那两段里有多少个深色像素（字）：现在是 **0 / 0**（相邻文字都被推到带子两侧）、
  被改内容自己那两行 360 个。`--ink-negative` 把旧的画法（用负 margin 抵消空档）临时放回来再量一次：
  同一位置立刻数出 **201 个字**——尺子自己也验一遍，免得"✓"只是量了个寂寞
- **记录页那三条分隔条是拖出来的**（`probe-fix-align.mjs --records`，真实浏览器）：
  连拖三条之后原文栏 250.5px、译文栏 387.8px——都在正常量级。
  这条必须用真浏览器量：布局是纯 CSS 的事，jsdom 不排版，量不出"译文每行只剩一个字符"那种毛病

**尚未开始**

- D1 数据库（题库与练习记录现在存在**这台浏览器**的 localStorage 里：
  刷新不丢，但换台电脑就没有；接 D1 后改为读服务端）
- 批改的用量上限（站点对所有人开放，任何人都能花这份余额；想加就在 Cloudflare 那一层加
  限速或 Turnstile，不必改代码，见 ADR 0027）

> ⚠️ 密钥安全：项目里的密钥只存在于 `.dev.vars`（已 gitignore）。若密钥曾在任何对话或截图中出现过，
> 请到 platform.deepseek.com 吊销重发。
