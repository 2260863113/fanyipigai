# 翻译批改（fanyipigai）

为「外研社·国才杯」笔译赛项练习转录服务的领域模型。这里只定义词汇，不含实现细节——判断一个词该不该收录，问自己：它是本项目特有的概念，还是通用编程概念？只有前者属于这里。

## Language

### 练习对象

**源文本**：
被翻译的原始文字。一篇源文本可以派生出多道题目，因此它独立于题目存在。
_Avoid_：原文、素材、文章

**题目**：
一次练习的最小单位，由「源文本 + 题型 + 方向 + 文体」共同定义。一道题目可以反复练习，每次练习各留一条记录。
_Avoid_：试题、任务、exercise

**自定义题**：
用户自己贴进来的**题目**：只给源文本，方向与题型由程序按文字判断，参考译文留空。
**源文本就是原文栏里那个输入框**——进这一栏不再弹窗问一次，用户边写边看（第 13 轮）。
它同样留下**练习记录**，但没有备选原文与 AI 出题——那两项是内置题才有的。
_Avoid_：粘贴题、我的题、custom

**术语范围**：
术语栏**自己那张封闭的表**，与**话题领域**是**平行**的两张表，取值**五个板块**：国内机关名称、国际机关名称、当代术语、必背核心术语、必背用典。
它**不隶属**话题领域——机关名称与手册里那批术语都没有社会／经济／文化／生态／科技这种板块属性，
硬塞进那张表就得替 1001 条术语凭空编一个板块，编出来的「领域」是假的（见 ADR 0025）。
一张范围就是一个**题库单位**：它有份数、有自己的**分组**、有页码（**一页十条**），因此术语题按它编号与分页。
⚠️ **一组几条是每个范围自己的数**（当代术语每 50 条一组，其余四个范围每 20 条一组），全站没有"一组几条"这一个统一数。
界面上那颗按钮写「范围」，不写「领域」——「领域」这个词全站已经专指那五个板块了。
_Avoid_：术语领域、词库分类、term domain

**分组**：
一个**术语范围**内部的切分，屏幕上的名字形如「当代术语|第1组（1-50）」（"第1组"在五个范围里各有一个，所以名字要带上范围）。
它是**入口与落点**，**不是题目单位**：用户按组挑"这一段"，点「确定」落到**这一组的第一页**，
此后的练习、判分、记录仍然一页一页地走。
它与**页**是两个不同的数：**组大小逐范围不同**（当代术语 50、其余 20），**页固定十条**；
"一组 20 条正好两页"只是 20 能被 10 整除的算术结果，不是一条规矩（末组不满时页数自然少）。
⚠️ **组号不进题号**：题号仍是 `term-v3-<范围>-<方向>`。记录与收藏**只存题号、不存题干**，
组号一旦编进题号就是一次换代，用户手里那些记录会全部回查不到内容（见 ADR 0030）。
_Avoid_：小节、章节、批次、第几套

**题型**：
题目的四种形态之一：术语翻译、句子翻译、段落翻译、文章翻译。它们要求的批注能力不同——术语题不存在语序调换，文章题需要管理篇长与段落切分。界面上以「文章 / 段落 / 句子 / 术语」四个标签呈现在顶部导航栏。
_Avoid_：练习板块、模块、类型、kind

**方向**：
翻译的走向，只有两个取值：中译英、英译中。它决定字数口径（英按词数、中按字符数）。
_Avoid_：语言对、target language

**文体**：
源文本的语言体裁，取值为官方考查的四类：政治文献、新闻编译、文学作品选篇、一般说明文。它与**话题领域**是两个不同的维度——前者决定文字怎么组织，后者决定写的是什么。
_Avoid_：文类、genre、体裁

**话题领域**：
源文本谈论的主题范围。**文章库里它是一张封闭的表**，取值为**五个板块**：社会、经济、文化、生态、科技。
这张表**文章栏与句子栏共用**——文章库按它组织、句子题的领域下拉用它、AI 出题的预设领域也照它排。
政治建设、教育强国、国际传播三个领域已整个删掉（见 ADR 0010；那条 ADR 里写着想恢复时该从哪儿动手）。
界面上就叫「领域」。**自己贴的题**没有领域——用户自己贴的原文不属于任何板块，也不要为此硬塞一个。
⚠️ 第 13 轮起**术语栏不再用它**：术语题按**术语范围**开题（另一张平行的封闭表，每个范围内部再分**分组**，见上）。
早先这里写着"术语库按它分组"，那一条随术语库整批换成机关名称材料而作废（见 ADR 0023）。
它与**文体**是两个不同的维度：前者决定写的是什么，后者决定文字怎么组织。
_Avoid_：题材、分类、category

⚠️ **「真题」与「样题」不是话题领域**（ADR 0029）：它们是**卷子的来源**，
只出现在**文章栏**那张更长的表里（`ARTICLE_BANK_DOMAINS` = 上面五个 + 真题 + 样题）。
句子栏、术语库、AI 出题一律只看五个话题领域——"真题"不是话题，列在那里只会多出永远没题目的格子。
每篇真题/样题的题号是 `art-past-paper-<方向>-<第几篇>` / `art-sample-<方向>-<第几篇>`，
`title` 末尾写着它的**来源文件**（哪一年、哪一份卷子）。
_Avoid_：卷种、题库、exam domain

**判分来源**：
一次**批改**是谁判的，只有三个取值：
**AI 现场批改**（唯一会花钱、会等的那一种）、**内置示例批改**（离线演示，**不是真的批改**）、
**本地判分**（术语题：程序按官方译名对照，不经模型、不用等、不花钱）。
它**不是分数**——本地判分与 AI 批改的分数量纲本来不同（前者是"这一页错了几个词"）。
⚠️ 界面上的差别只有一处：**内置示例批改**要在结果栏里明说一句，另两档不必
（顶栏那枚常驻的示例警告芯片已按用户要求删掉：术语本地判分当时借用了示例的身份，
于是每次术语批改都冒出来；现在它有自己的来源了，见 ADR 0023）。
_Avoid_：分数来源、模型、judge source

**事件锚点**：
文章库每一篇自带的那**一行背景说明**——它说明这一篇讲的是哪件事（时间、地点、发生了什么），
例如"2026 年 3 月，十四届全国人大四次会议民生主题记者会披露社区养老服务扩面计划"。
它同时充当**选题卡片上的标题**；练习页**不显示**它（练习页只有原文，那才像考场）。
它与**标题**不是一回事：这些材料不是新闻，没有原标题，也不该替它编一个。
_Avoid_：摘要、题记、简介

**文章库**：
预置的**成篇练习材料**，按「领域 × 方向」组织，每个格子若干篇，供练习时挑选。
与内置示例题、AI 出题并列的第三种题目来源。每个格子 5 篇，唯**生态 / 英译中**是 3 篇
（那批材料里三篇讲的是同一件事，去重后只剩 3 篇）。
它是**随代码进仓库的固定数据**，源材料是仓库根目录的一份 docx、正文由脚本从它现算。
**每篇自带参考译文**，与原文**逐段一一对应**，因此可以逐段对照。
_Avoid_：题库、素材库、语料库

**篇长**：
题目对源文本长度的硬性要求，按方向取不同口径：英译汉 250–350 词，汉译英 200–300 字。它是**校验条件**，不是建议。
_Avoid_：字数要求、长度限制

### 作答与批改

**作答**：
用户针对一道题目写下的一次译文。同一道题目可以有无穷多次作答。
_Avoid_：答案、提交、提交内容

**批改**：
对一次作答的完整评价，包含分数、评语、错误列表与亮点列表。它是作答的产物，一次作答对应一次批改。
_Avoid_：评测、评分、review

**错误对象**：
批改中描述**一处**问题的最小单位。每处问题同时携带一个「改法类型」和一个「错误分类」。
_Avoid_：错误项、标注、annotation

**改法类型**：
错误对象说明"该怎么改"，只有五种：替换、插入、删除、整句重写、语序调换。它决定这处错误在页面上如何呈现。
_Avoid_：操作类型、动作、op

**错误分类**：
错误对象说明"错在哪里"，取值来自一张固定的分类表（术语不准、漏译、增译、**语法**、语序错、搭配不当、用词不当、语体不符、冠词介词单复数、标点）。**一条错误只归一类**，按固定优先级判定，否则统计出的弱项会变成同义词碎片。

其中「语法」与「冠词介词单复数」属于硬性错误（红色）：「语法正确、只是用词不准」才允许归到用词不当；固定术语只要错一个词，就算术语不准。
_Avoid_：错误标签、错误种类、原因

**亮点**：
作答中表达优秀、值得肯定的片段。它**不是错误**，因此独立于错误对象存在，不参与扣分，也不计入错误统计。
_Avoid_：正确项、加分项

**断句**：
把一段作答文字切成"句子"的那条判据。**全站只有一份**（`domain/sentences.ts`），三处都在用它：对照视图要按句给出「改前 / 改后」、句子题要按句取题、收藏要取"这一处所在的那一句"。三者的差别只有一处——**收藏把逗号也算句末**（它要的是"那一小截"而不是语法上的整句），其余判据（含"句末标点后面跟引号也算断句"、"句号后面**漏了空格**也算断句"）必须完全一致。最后那一条与提示词同一个口径：AI 不许因为少一个空格扣分，程序也不该因为少一个空格把三句读成一句。
_Avoid_：分句、切句、sentence split、句子切分

**批改视图 / 对照视图**：
同一份批改的两种看法。批改视图把改动**画在译文上**（荧光底色、上方补写的字、调序弧线），点哪一处看哪一处；对照视图则**按句**给出"原译 → 改后"，不划线不填补。两者共用同一个偏好设置，因此练习页与练习记录页里的"同一个开关"始终是同一个视图。
_Avoid_：展示模式、渲染模式、view mode

**锚点**：
错误对象在作答文本中指向的位置，由「字符序号区间 + 该区间的原文片段」共同描述。片段的作用是让程序能自证位置是否有误——AI 数错位置时，程序会发现并拒绝渲染。
_Avoid_：位置、offset、坐标

**批注卡片**：
点一处批注之后出现的两块说明：**小卡片**（浮在那一行旁边，只说"这是什么问题、为什么"）与**大卡片**（右下栏，说全这一处：改前 / 改后 / 说明 / 收藏）。

**收藏**：
用户自己挑出来、留着回头复习的条目，存在这台浏览器上（`translation-practice.favorites`）。
它现在是**两种**（第 17 条起，见 ADR 0031）：
- **某处批注**（精修档点译文上任意一处勾画，或上方补写的字）：改前 / 改后 / 为什么，
  外加"这一处所在的那一整句"；
- **整句收藏**（大改档每句原文后面那颗「收藏」）：**原文 / 我的译文 / 修改译文 / 解释**四部分——
  大改是整篇逐句重写，用户要复习的是"这一句我原来怎么写、AI 改成什么样、为什么"。
两种共用一个列表、一个存储键、一套去重与删除；同一处再点一次就是取消。
_Avoid_：书签、标记、笔记
两张卡都带"卡片"标记（`data-card`），因此**点卡片里任何地方都不会把它们收起来**——只有点这两张卡片**之外**的地方才收（用户要求）。
小卡片用**固定定位挂在 `document.body` 上**：它不是栏里的内容，而是一块浮层，因此不会被译文栏的滚动切口剪掉、也压得住下面那两栏（同上，用户要求）。
_Avoid_：气泡、浮层、popover、tooltip

**修改风格**：
批改的力度档位，只有两档：**精修**（逐处批注：指出硬性错误与严重表达不当）、
**大改**（换个做法：把整篇译文**逐句重写**，每句给出为什么这么改，并给出这一句对应的原文）。
**两档的产物不同，但分数只有精修档有**：大改**不打分、也不给总评**（见 ADR 0020），
因此全站只有一个分数口径，也就是精修档那个"程序按错误算、你能自己核"的分数。
⚠️ **这两个名字换过一次**（用户要求："将'润色'改成'精修'，'精修'改成'大改'"）：
界面上的「精修」原先叫"润色"（代码里的键是 `polish`），「大改」原先叫"精修"（键是 `refine`）。
**内部键一律没动**（它们写进了解析、提示词与落盘记录），因此读代码时以键为准：
`refine*` 在代码里指的都是**大改**，不是界面上那个「精修」。
_Avoid_：模式、level、强度

**逐句改写**：
大改档的产物：把学生的译文**以句子为单位**重写成更好的一版，每一句都说明为什么这么改，
并且每一句都要给出**对应的原文那一句**（界面把它排在最上面，见 ADR 0020 与第 4 条）。
**没改的句子也照抄一遍列在里面**——不是啰嗦，是为了让程序把整篇对齐（见 ADR 0015）。
它与**错误对象**是两条路：大改**不逐处批改**，因此没有勾画、没有错误归类、没有弱项统计。
_Avoid_：重译、整篇精修、rewrite

### 内容与分数

**参考译文**：
题目的参考答案。它**随题目确定一次后固定下来**，不随每次批改变化——否则同一道题前后两次的分数无法比较。
文章库那 48 篇的参考译文**随材料进仓库**（与原文逐段对齐），不靠 AI 现生成；其余题目由 AI 生成后固定。
它**只给人看、从不发给模型**（见 ADR 0003 的补充）。
界面上的入口是**正文末尾那四个字**：点一下把**当前这一页**的译文铺在原文下面，再点一下折回去。
（原文栏标题栏里那颗「对照译文」按钮已按用户要求删掉——它与这个折叠块是同一件事的两种做法，
留着只会让人在两处找。没有译文的题连这四个字都不出现。）
_Avoid_：标准答案、范文、答案、对照译文

**术语表**：
术语翻译题的判定依据，收录**术语范围**下的官方译名及其一并算对的写法。
它是**随代码进仓库的固定数据**，共 **1001 条**，材料来自两处：仓库根目录的两份 docx
（`国内机构.docx` 83 条、`国际机构.docx` 60 条）与 `handbook-src/` 里整理好的
《理解当代中国 核心术语学习手册》条目（当代术语 718 条、必背核心术语 60 条、必背用典 80 条）；
正文分别由 `scripts/build-terms.mjs` 与 `scripts/build-handbook-terms.mjs` 现算，
**不要手写那五个数据文件**（与**文章库**同一套做法，见 ADR 0023 与 ADR 0030）。
每条术语两侧都有（中文名 + 官方英文名），因此**两个方向都能练**。
「标准答案」不是一条而是一**组**：官方缩写、英美拼写、重音符号、开头的 The 都不计较
（见 ADR 0024）；同一条官方英文对应两条中文时（一英多中）**写哪个都算对**，
界面上把两个中文用「／」一起列出来；手册那三个范围的条目里**末尾句号写不写都算对**
（屏幕上显示的仍是带标点的原样，见 ADR 0030）。
⚠️ 术语表**不再按话题领域分组**，也**不再有"译法待核对"这个状态**——
整批材料取的是官方公布或权威渠道发布的译名；早先那 43 条按板块分组的政论术语
已**归档**（`src/domain/legacy-terms.ts`，不被任何代码引用，见 ADR 0023）。
_Avoid_：词库、词条表、glossary

**分数**：
一次批改的综合结论，0–100，**只有精修档有**（大改不打分，见 ADR 0020）。
它**由程序按错误列表算出来，不由 AI 给**，因此与页面上实际标出的错误永远一致，
不会出现"90 分却标了 8 处硬性错误"这种自相矛盾。三种扣法并存（见 scoring.ts 与 ADR 0021）：
**漏译按字数扣**（漏多少字/词就扣多少分，不再另扣固定分）、其它硬性错误每处 8 分、表达问题每处 3 分；
亮点不加分（加分会让"多写亮点"变成刷分动机）。
漏译扣了多少会**单独写一行**（"漏译扣 X 分（漏 N 个单位）"），用户才核得出来。
评价尺子固定为比赛评分标准。
_Avoid_：维度分、分项、指标、子分数

**最高分**：
「选择文章」每张卡片上那一枚「最高分 N 分」：这篇文章历次批改里**最好的一次**（按题号取最大值，
一篇文章好几页，卡片刻度是"这一篇练到过的最好水平"）。没练过、或没得分的显示 **0 分**而不是把标签藏起来——
藏起来会让"还没练过"和"练过但没得分"看起来一样。
**只算精修档**：大改不打分（用户拍板"分数打分只有精修部分有"），因此不再有"两种分数混在一起比大小"这件事。
_Avoid_：最好成绩、记录分

**红橙两色**：
一处批注的颜色。红＝硬性错误、橙＝表达问题。**多数分类由分类推导**（同一类错误每次都是同一个颜色，
用户才能形成阅读习惯），但**漏译与多译例外**：它们的轻重由程序按字数判——
漏/多不足 3 个单位算橙，3 个及以上算红（见 ADR 0014）。
颜色的**唯一来源是解析时算好的那个结果**（`ValidatedError.hard`），下游一律读它、不按分类现推。
⚠️ 颜色与**扣分**是两件事：漏译的颜色仍按上面这条判，扣分则按字数算（见**分数**与 ADR 0021）。
_Avoid_：严重程度、优先级、色阶

**啰嗦 / 更好的表达（两遍自查里的说法）**：
英译中·精修的提示词按用户指定的顺序走五遍：**漏译 → 术语 → 啰嗦 → 更好的表达/用词 → 语法与标点**。
「啰嗦」是一个**分类**（`verbosity`，橙色）：绕圈子、重复、可有可无的堆砌；
「更好的表达/用词」不是新分类，落到原有的 word-choice / collocation / register / word-order。
⚠️ 这五遍是**检查顺序**，不是**归类优先级**：同一处同时像两类时，仍然按分类表取靠前的
（硬性错误优先于表达问题）。中译英·精修仍是原来的四遍（用户明确"只给英译中"，见 ADR 0021）。
_Avoid_：润色、优化、重写（大改是另一档的事）

> **为什么没有"维度分"**：早期设计是让 AI 输出四个维度分（术语准确、语法正确、语篇连贯、语体得体），现已废止。
> 理由有三：**一致性**（分数与标出的错误必然对得上）、**可解释**（用户能自己核出这个分数怎么来的）、
> **成本**（提示词里少一块内容，返回更短、更不容易被截断）。
> 维度分的诊断价值由**错误分类的计数**承接——它同样能看出拖后腿的是术语还是语法。
> 详见 ADR 0005 的后续补充。

**页**：
一道题切好后，**用户一次看到、一次译、一次交**的那一段。原文栏一次只显示一页，作答栏一次只写一页——
它是**逐页批改**的单位。文章题与术语题都有页：
**文章题一页就是一个自然段**（不足 50 单位（英译中计词、中译英计汉字）的自然段与相邻段并成一页，
长段**不切**，规则见 ADR 0012）；
**术语题一页就是十条术语**，末页可能不满十条——**照旧等分，缺的行留空、不可填也不计分**（用户拍板）。
它与**分组**是两件事：页是批改单位、全站一个数，分组只是入口与落点、逐范围不同（见**分组**）。
两者的页都落在同一个页号空间里（`sectionIndex`），因此翻页、页状态、进度、批改记录全都共用一套。
_Avoid_：段（多数时候一页等于一段，但不足下限的短段会与相邻段并在一起）

**文章进度**：
某一篇文章（或某一个**术语范围**）**已经批过哪几页**这份事实，存在浏览器里，与**练习记录**是两回事
（记录存的是每次批改的完整内容，进度只记"这一页做完了没有"）。它决定两件事：**上次的界面**没记着时，
下次打开**从没有翻译完成的那一段继续**；整篇（或整个范围）练完的不再被主动打开、
在「选择文章」与「换一换」里排到最后（术语范围则会让这一栏落到另一个范围去）。
**它只增不减**（用户拍板）：按「返回编辑」放开重写**不会**把这一页撤掉——"批过就算批过"。
唯一会撤掉它的是**删掉这一页最后一条批改记录**（那一次是真的不存在了，见 ADR 0019）。
⚠️ 界面上的「已批 N 页」芯片已按用户要求删掉；删的是**显示**，这份进度照旧在管落点。
_Avoid_：草稿、断点、draft

**上次的界面**：
上次关掉网页时停在哪——**栏 + 题 + 来源 + 页**，存在浏览器里，下次打开就落回那里（见 ADR 0013）。
它与**文章进度**回答的是两个不同的问题（"上次看到哪儿"对"哪几页做完了"），因此可能给出互相矛盾的页号：
以**上次的界面**为准，"从没批完的那一段继续"只在它没记着时才用。
"来源"（文章库 / 内置题库 / AI 出题 / 自己贴的题）必须一起记——四者共用同一个题号空间，
只按题号取回来可能取到另一个来源的同号题。记录页与收藏页**不记**：那是去看别的东西，不是练习位置。
_Avoid_：断点、草稿、恢复现场

**逐页批改**：
**文章题与术语题**共同的工作方式：一页一页译、一页一页交。**翻页只是翻页**——点「上一页 / 下一页」既不提交、也不拦住人，
当前页的草稿按页留着，回来接着写（第三版起如此，详见 ADR 0017）。提交只有**一个**触发点：用户按「提交批改」。
交出去之后这一页只读（作答框 `readonly`、按钮写着"批改中…"），但**翻页不拦**：可以去下一页接着译，
批完在右下角的**批改完成通知**里喊一声。已经批过的页只读，要先按「返回编辑」才能改。
一次只批一页。
⚠️ 术语题**整套照搬**了这套规矩（第 13 轮用户要求"像文章模式一样一页一页翻"），
差别有两处：术语判分是**本地**的，回来的瞬间就有结果，因此不会有"批改中"那一档与那条通知
（它的页状态只经过 待批改 / 已批改 / 编辑中）；以及**这一页没写满也能提交，只是不产生练习记录**
（判分照做、结果显示照做、"哪几页批过"的进度照记，**只有练习记录**那一步按是否十条都写了决定，
判据只有一处 `termPageComplete`）。
_Avoid_：分段批改（那是"把一篇切成几段并行提交"，机制完全不同）

**待批改 / 批改中 / 已批改 / 编辑中**：
某一页在逐页批改里所处的位置，界面上的翻页导航会如实报出当前是哪一档，因此这四个词在界面文案与代码里
是同一套说法（`PageState`）。"批改中"专指**已经交出去、结果还没回来**的那一页（只读 + 进度条）；
"编辑中"专指**按过「返回编辑」、正在改写**的那一页。
⚠️ 标题栏那颗按钮在这两档之间**换的是名字，不是位置**（用户第 2 条要求）：
批改后它写「返回编辑」，点下去回到可写，名字随即变回「提交批改」——同一颗按钮、两个名字。
**文章栏与术语栏是同一颗按钮、同一个动作**（第 13 轮把术语栏那颗「重新作答」并了进来：
那个动作会**真的丢掉结果、撤掉进度**，与文章栏的「返回编辑」不是一回事）。
第三版之前的"已修改待提交"没有了：它唯一的用处是决定"翻页要不要自动提交"，而自动提交已经取消。
⚠️ **「返回编辑」只管你人还站在这一页上的时候**（用户要求："批改后的状态，切换其他页再回来，
也需要在批改界面，不能回到编辑界面"）：没改字就翻页或换栏走开，这个标记就被收回，
回来看到的仍是那份批改；改过字再走则保留——那时结果已经作废，它是一道真正待提交的题。
_Avoid_：未提交、草稿、dirty、已修改待提交

**批改完成通知**：
右下角弹出来的那条「第 x 页已经批改完成」。点它**跳到那一页**看结果；人自己翻到那一页时它自动消失。
它存在的前提是"批改期间允许翻页"——既然允许交了就走，结果回来时必须有人喊一声（见 ADR 0017）。
同时只可能有一条（一次只批一页），因此不排队。
它与**吐司**是两回事：这条是"另一页的事跑来找人"、要等人点；吐司是"刚才那一下为什么没生效"，看完就走。
_Avoid_：提示、弹窗、toast、消息（那是别的通知）

**吐司**：
底部居中、几秒后自己消失的一句提示。两个用处：**提交被提交门拦下时**把原因原样说出来
（"还差几个单位"／"与之前某次一字不差"），以及**一次性清理换代之前那些术语数据时**说一句
（哪几条记录、哪几条收藏被清掉了——那是用户自己要求删的，但删了要留个交代）。
它不改变任何状态——拦下就是**什么都没发生**。
_Avoid_：提示条、消息、通知

**提交门**：
按下「提交批改」之后、**发出请求之前**的那两道判据（用户第 3 条要求）：**篇幅下限**
（文章题与段落题必须多于 30 个单位，中译英数词、英译中数汉字）与**去重**
（去掉首尾空白后与这一段之前任何一次提交逐字符完全相同就算重复）。
任何一条不过就**什么都不做**：不标记批改中、不发请求，只弹一条**吐司**说明原因。
去重比对的是"练习记录里**还留着**的每一次提交"，因此删掉的记录不再拦人（见 ADR 0018）。
_Avoid_：校验、表单验证（那是"字段格式对不对"，与内容无关）

**批改记录**：
译文栏标题栏里那个下拉：**这一页之前每一次批改**的清单（最新在前，标"第 N 次 · 时间 · 分数"），
选哪一次就把右栏切成那一次的结果。它读的是落盘的**练习记录**，因此**改过字之后照样能回看**
（这一点是它取代旧「查看上次批改」按钮的理由，见 ADR 0016）。
只读的已批改状态下也显示——批完就停在这一页，回看不必先按「返回编辑」。
每一条右边的**叉号**可以删掉它：练习记录与这个下拉读的是同一份数据，因此"删一处、两处都没了"
（用户要的"练习记录同步删除"不需要第二份代码）。点叉号先就地确认一次；删完撤掉**文章进度**里的那一页、
正在看的就是它时退回最新一次（一条都不剩就退回作答框），见 ADR 0019。
_Avoid_：历史、版本、存档列表

**练习记录**：
一次作答与其批改共同构成的存档，用于回顾与弱项统计。不含趋势图表。**逐页批改之后一条记录 = 一页**（带页号），不再是"整篇"。
它同时是**唯一落盘的批改**：会话（作答、结果、页号）只在内存里，因此刷新或页面重载之后，
"批改过的页"靠它接回会话——打开时把"记录里有、会话里没有"的那几页接回来（每页取最新一条，草稿一并补上），
于是刷新之后翻回那一页看到的仍是那份批改，而不是一张空作答框。这正是用户说的"批改后的段落回来还是批改界面"。
_Avoid_：历史、日志、记录

**接回会话 / 会话还原**：
打开一道题时，把**练习记录**与**页状态**放回会话的那一步。它只在**这道题本次打开还没有会话**时做，
因此不会把用户刚作废掉的结果复活，也不会盖掉正在写的草稿（见 `sessionRestored`）。
⚠️ 一条例外：**页状态里记着"这一页在编辑态"的那几页不接记录里的结果**，只接草稿——
用户对这个问题的答复是"刷新之后旧批改不算了，自己去「批改记录」下拉里重新调出来"（见 ADR 0019）。
接回来的那一步会顺手把会话建起来，因此**起始页号必须在这里给对**，否则"打开就落在上次那一页"会被抹掉。
_Avoid_：恢复、同步、加载存档

**页状态**：
某一页**此刻的界面状态**：写到哪儿了（草稿）、是不是在编辑、**正在看第几次批改**。
三样都按「题 + 页」存在浏览器里，因此翻页往返、刷新、重开浏览器都还在（见 ADR 0019）。
它与**练习记录**分工明确：记录存的是"批过什么"（事实），页状态存的是"这一页此刻长什么样"（姿势）；
换原文时页状态要整道题清掉，否则旧原文的草稿会贴着新原文回来。
_Avoid_：页面快照、断点、临时数据

**分割线位置**：
四栏之间那几条可拖动边界的比例。三组各记各的（练习页、练习记录页外层、练习记录页内层），
**双击恢复默认也写回存储**——用户的口径是"你走的时候什么位置，下次回来还是在那个位置"，
因此"自动布局"本身也是一个要记住的位置（见 ADR 0019）。
_Avoid_：布局设置、栏宽

**暗夜模式**：
整站的深色配色。默认**跟随系统**（系统是深色就深色），导航栏上那颗按钮手动切过之后
就不再听系统的——"我按过了"比系统设置硬，否则用户在深色系统上切回浅色、下次打开又被改回去。
配色换的是一套**令牌**（`styles.css` 里 `:root[data-theme='dark']` 那一块），组件一行都不用改。
_Avoid_：主题、皮肤、深色模式

### 账号

**账号**：
一个用户名 + 一份密码哈希。密码**明文从不上网**：浏览器取随机盐、算 12 万次 PBKDF2-SHA-256，
服务端只存盐与结果（见 ADR 0028）。因此服务端**没有**、也不该有"用户的密码"这个东西——
忘记密码只能重来（没有邮箱，「找回密码」在「明确不做」里）。
_Avoid_：用户（那会让人以为站内有成员体系）、member

**会话凭证**：
登录成功后服务端发的那串 32 字节随机十六进制，存在浏览器里、有效期 30 天。
**库里只存它的 SHA-256 摘要**，因此库被看到也拿不到能用的登录态。
它与**账号**是两回事：账号是"你是谁"，凭证是"这一次证明你是你"，可以同时有好几张（多台设备）。
_Avoid_：token、登录态、cookie（它不是 cookie，走的是 `Authorization` 头）

**登录门**：
"没登录就不许提交批改"这条规矩。界面上表现为**按了「提交批改」什么都不发生**——
不标记批改中、不发请求，只弹出登录/注册窗并写清原因；服务端那一侧由
`requireSession` 兜底（`/api/judge` 前面那道检查才是真的边界）。
浏览（选题、看原文与参考译文、看留言板）**不受它管**。
它的位置在**篇幅门与去重门之后**：先告诉用户"这段本身行不行"，再谈"你得先登录"。
_Avoid_：鉴权、权限校验、auth（那些词会把"能不能看"混进来）

**留言板**：
一个人人可读、登录可写的公告栏（帖子 ≤200 字、回复 ≤100 字，只能删自己的）。
它与**练习记录**无关：记录是"你自己练了什么"，留言板是"说给大家听的"。
_Avoid_：评论、论坛、社区

**访问日志**：
每次打开站点记一条：**谁（登录名或游客）+ 什么设备 + 什么时候**，**不记 IP**。
它是给站主自己看的（管理页的「日志管理」那一屏），用途是"这站有没有人在用"，
不是审计——真要审计也不会只有这三样。
**"什么设备"存的是原样的 User-Agent**（游客也一样存），屏幕上把三样一起给出来：
短名字（`Chrome · Windows`，扫描用）、**设备名**（带版本与机型，见下）、以及完整的那一串原样。
_Avoid_：埋点、统计、PV/UV

**设备名**：
从 User-Agent 里认出来的"什么浏览器 + 什么系统 + 什么机器"，写成人话的样子：
`Edge 140 · Windows 10/11 · 电脑`。它与**原样的 User-Agent**是两个东西——
前者是给人一眼扫的（带版本号与机型），后者是完整的证据（可复制、可核对）。
认不出来时照实写"未知浏览器 / 未知系统"或原样截断，**不假装认出来了**（不引 UA 解析库）。
Windows 的 UA 只给 `Windows NT 10.0`（11 与 10 在 UA 上分不开），因此写「10/11」而不是猜一个。
_Avoid_：浏览器指纹、UA 解析

**账号下拉栏**：
顶栏右侧那颗「头像 + 用户名」点开之后的那个菜单：**个人中心 / 修改密码 /
（管理员才有）用户管理 · 日志管理 · 发布公告 / 退出登录**。
它是这一组页面的**唯一入口**——导航里不再有常驻的「管理」（第 17 条，见 ADR 0031）。
"当前在看管理页的哪一屏"由它决定，因此点「用户管理」是**直接落到那一屏**，不需要再点一次。
_Avoid_：用户菜单、侧边栏

**公告**：
管理员发布的告示（标题 ≤60 字、正文 ≤2000 字，纯文本，可置顶）。
**谁都能读**——它排在留言板的最上面，按"置顶优先 + 时间倒序"。
它与**留言板**是两回事：留言板是大家写的，公告只有管理员能写。
_Avoid_：通知、消息、banner

**管理员**：
`is_admin = 1` 的账号，能看用户列表与访问日志。**没有自助入口**：
注册接口一律写 0，只能由站主用 SQL 指定（一条命令，见 README 的部署章节）。
_Avoid_：站长、超管、root

### 部署

**宿主**：
"这一半代码跑在什么上面"的统称：本地是 **Vite 开发服务器**（改 `src/domain/*.ts` 下次请求就生效），
线上是 **Cloudflare Pages Functions**（workerd，没有文件系统）。
三个接口的实现**只有一份**（`src/server/api.ts`），两边各自只提供"密钥从哪来、日志往哪写、
失败记录往哪存"。因此判断"这个改动要不要两边都改"时，先看它属不属于宿主那一层——
不是宿主的事，两边一起变；是宿主的事，就只在 `vite-plugin-judge-api.ts` 或 `functions/` 里改。
_Avoid_：运行环境、平台、runtime

**访问控制**：
**没有**。这个站对所有人开放：谁拿到网址谁就能批改，也就等于谁都能花站主的 API 余额
（用户 2026-09-25 明确要求"去掉进入口令这一环节，任何人都可以访问"，见 ADR 0027）。
它曾经有一道口令门（口令 + 签名 cookie，见 ADR 0026），那套实现留在 git 历史里。
因此现在问"要不要登录"这个问题的答案是"从来不需要"；要加闸的话加在 Cloudflare 那一层
（限速规则 / Turnstile），不改这个仓库。
_Avoid_：登录、账号、权限（这些词会让人以为站内有用户体系）

### 失败与诊断

**失败存档**：
批改未能完成时，对当次 AI 返回与请求上下文的留档。它与**练习记录**是两回事：练习记录存的是用户的作答与批改结果，失败存档存的是"AI 没给出可用结果"这件事本身，用途是改进提示词，不面向用户展示。
_Avoid_：错误日志、debug 记录

**失败类型**：
失败存档的分类，取值为：格式不合法（返回的不是可用 JSON）、位置对不上（批注位置与译文全都不符）、被截断（输出超出单次上限）、重试用尽（多次尝试后仍不合格）。分类的目的是让"该改提示词的哪一部分"一眼可见。
_Avoid_：错误码、error kind
