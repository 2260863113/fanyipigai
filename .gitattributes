# 统一按 LF 存储，避免 Windows 与 Linux 之间反复出现"整文件被改写"的提交。
# Cloudflare 的构建在 Linux 上跑，本地在 Windows 上开发，这个约定能省掉一类无意义的冲突。
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary

# Windows PowerShell 5.1 不带 BOM 时会按 GBK 读取 .ps1，中文与引号会被解码错乱，
# 导致脚本直接语法报错。因此标记为 binary，让 git 原样保留文件里的 BOM。
*.ps1 binary
